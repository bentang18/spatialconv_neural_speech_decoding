# v14 Architectural & Training Blockers

Living list of every under-specified piece of the v14 spec. Walk through one at a time. Resolved gaps get a ✅ + "Decision" line + date. Source: post-v3 amendment (`memory/project_v14_arch_post_v3_amendment_2026_05_19.md`, canonical), 5/22 pivot (`memory/project_v14_imindbench_multistft_pivot_2026_05_22.md`, wins on conflict), 3-phase recipe (`memory/project_v14_three_phase_staged_recipe_2026_05_18.md`), cross-subject pretrain strategy (`memory/project_v14_cross_subject_pretraining_data_strategy_2026_05_22.md`), `plan.md`, [[project_v14_b29_joint_default_2026_05_27]] (B29 joint-default + Items 11/12/13/14/15, **wins on conflict for phase structure / corpus mix / slot count / gating / capacity sisters**), [[project_v14_moe_ffn_audit_2026_05_28]] (MoE-FFN deferred v15).

## Status (2026-05-28 PM post-B31 V-JEPA-2-canonical 2-term SSL + PMA-trained-only-at-P3 lock; post-B30 anatomy-gated-symmetric partial revert of B29 Item 12)

**Counts**: 186 open / 27 ✅ resolved-retained / 26 locked-into-recipe-and-deleted (→ `training_recipe.md §9`) / 1 promoted (IE01 → B17). **+4 new B30 code-vs-spec rows; B29-no-gating SUPERSEDED in place by B30-anatomy-gated-via-latent-valid (kept marked-superseded for provenance); + B30-dispatch-sister-flags row added 2026-05-28 from R12 wiring audit; + B31-loss-2-term row added 2026-05-28 PM (V-JEPA-2-canonical default collapse from 4 → 2 SSL terms + PMA-trained-only-at-P3) → 24-row drift table (was 17, then 21, then 22, then 23; +1 B35-p4-frozen-pma-mean-linear row added 2026-05-31 — **REVERTS the never-run B34 attentive-query P4 swap**; the B34 P4 code that LANDED uncommitted now DIVERGES from B35 and must be rewired to frozen-PMA→mean→linear (PMA returns to P4, frozen). B07/ARG03-pma-timeline: the P4-frozen-PMA half is RESTORED by B35; only the P1/P2 training half stays superseded by B31). +19 drift-table rows flipped to ✅ CLOSED 2026-05-28 (B20-Conv2d, B20-hop256, B27-L1, B28-DKoleo, B28-cross-attn, B28-anatomy-warmup, B29-subtype-embed, B29-m1-default, B29-d-bump-384, B29-no-moe-default, B29-joint-collapse, B30-sa-bidirectional-mask, B30-loss-rename-slot-mask, B30-pma-zero-active-guard, B30-dispatch-sister-flags, B03-shaft-mask, per-corpus-notch, REF-01, REF-02) + B30-anatomy-gated-via-latent-valid partial closure. **B31-loss-2-term CLOSED 2026-05-29 (code-vs-spec): the `loss_variant` selector + 2-term default aggregator + conditional LN_mid/LN_utt/PMA construction landed in `src/` with a 70-test green matrix; the P0 falsifier-sister RUNS remain a separate experimental gate.** → 1 row remaining open: B03b/f parcels_supervised (superseded by B30, retained for provenance only — no further code change required).**

**2026-06-01 B36 — encoder + SSL + phase redesign (`memory/project_v14_b36_perparcel_pool_structured_jepa_2026_06_01.md`; doc-only, all unwired).** B36 throws away the Perceiver and **changes the spec target for several rows the code had marked ✅ CLOSED against the B29/B31 design** — the code is not "wrong," it now implements a superseded design. Current `src/` (Perceiver soft-routing + B31 2-term aggregator + B03 shaft-mask + B29 joint phase) is the design B36 replaces.

*Spec-target SUPERSEDED (code implements a retired design — to be REPLACED, not completed):*
- **B28 cross-attn soft `log(support+ε)` bias + `λ_anat` gate + anatomy-bias warmup** → DELETE; replace with a hard **block-diagonal one-hot per-parcel pool** (query k attends only to parcel-k electrodes). `compute_gated_log_support_bias` soft path + `LambdaAnatExtractor` retired. (The `check_lock_drift.py` guard needs a B36 pass — its superseded-literal set assumes the soft-bias path exists.)
- **B31 2-term self-distill aggregator** (`L_pre_frame@M2 + L_post_frame@M4`, same input both sides) → REPLACE with paradigm-B masked parcel×time JEPA (separate predictor; loss on masked cells only).
- **B03 shaft-mask DROP** → REPLACE with **parcel×time block mask + upstream (electrode,time-patch) drop** (30% of covered cells; structured by parcel, not by shaft).
- **B29 joint P1+P2 phase** → RE-SPLIT: P1 front-end-only @ M2 (all corpora) / P2 pool+inter-parcel+predictor @ M4 (anatomy-only, front-end LR/10).

*New B36 code-vs-spec rows (all unimplemented):*
- **B36-per-parcel-pool** — harden cross-attn bias to a block-diagonal one-hot assignment mask (off-parcel = `NEG_INF`); drop the soft-bias/`λ_anat`/warmup path. `models/v14_encoder.py`.
- **B36-paradigm-b-predictor** — separate narrow transformer predictor (default **3 blocks @ d=128, 4 heads**, MLP 4×, ~0.6M, discarded at deploy) consuming visible parcel tokens + mask-token queries (parcel-id + time-pos); depth a P0 sweep {2,3,4}; `R-paradigm-a-mlp` = BaRISTA 5-layer FC sister.
- **B36-parcel-time-mask** — anatomy-structured (parcel × contiguous-time-block) sampler at 30%; masked parcels' electrodes DROPPED upstream in the token blocks (key-padding) so the front-end never sees masked cells (leakage-free); replaces `BTShaftMaskExtractor` on the default path.
- **B36-three-way-latent-valid** — extend B30 `latent_valid` two-way → three-way (visible=`covered&~masked` / target=`covered&masked` / teacher=`covered`); thread all three through encoder + predictor + loss.
- **B36-encoder-ln-target** — canonical V-JEPA target normalization: loss target = EMA teacher's post-`encoder_ln` M4 (stop-grad); DROP the separate B21/B31 `LN_frame` loss head.
- **B36-staged-phases** — P1/P2 split in `dispatch_v14.py` + `v14_joint_module.py`; discriminative LR (front-end LR/10 in P2); `R-joint-ssl` carries the B29 joint default as a P0 falsifier.
- **B36-rankme-monitor** — RankMe / effective-rank of the parcel-target batch logged from step 0 (low-cardinality dimensional-collapse tripwire).
- **B36-C5-freq-patch-valid-wiring** (WS-H, Ben-gated) — the SWEC corpus-valid freq-patch confinement (C5 of the 6/03 masking lock) is BUILT in the M2 sampler (`sample_token_band_mask(freq_patch_valid=)`), the front-end loss (`p1_frontend_m2_loss(freq_patch_valid=)`) and the encoder forward (`V14ParcelPerceiverModel.forward(freq_patch_valid=)`), but NOT threaded from `V14JointBrainModule` (`_extract_student_kwargs` / `_sample_phase_mask` / `_step` all leave it `None`). Deferred by design: (1) SWEC/AJILE12/D-cohort loaders are `NotImplementedError` (DP03) so no live path exercises it; BT (capstone) is all-valid ⇒ `None` ⇒ exact. (2) The batch-contract shape (shared `(F_p,)` vs per-clip `(B, F_p)` in a mixed-corpus batch — the M2 sampler currently only supports the shared prefix) depends on the unbuilt joint loader's batching policy. (3) The valid-bin RANGE itself (k0-21 vs k0-22) is a Ben-gated calibration decision. WS-H threads all three (key + shape + range) when SWEC P1 lands. Until then the live SWEC mask would silently place freq-bands in invalid k22-29 rows — call-site comments at the three module methods flag this.
- **B36-mon-mask-002-rewire** (WS-I monitor scope) — `_run_mask_orphan_monitor` (MON-MASK-002, the `orphan_MSE/visible_MSE ∈ [0.7, 1.5]` dispatch kill-criterion) is gated on a `shaft_mask` in the batch (`v14_joint_module.py` ~674), which B36 dropped from the default SSL path (WS-H5). So the monitor returns early and the kill-criterion is silently dead on the live B36 path. Diagnostic-only (never enters loss/grad), so it's not a correctness bug — but one of the 4 documented dispatch kill criteria no longer fires. WS-I must re-derive the orphan/visible-MSE analog under the tube mask (masked-parcel-target MSE vs visible-parcel MSE) or formally retire MON-MASK-002 from the kill set.

B33 (P3) and B35 (P4) rows untouched. Surviving pre-B36 rows B36 reuses (not replaces): per-corpus notch ✅, REF-01/02 ✅, B30 `latent_valid` derivation, B27 pure-L1 + EMA-fixed + teacher-full-input, B20 front-end. Numbers PDF-verified 6/01 (V-JEPA/2/2.1, Brain-JEPA, BaRISTA, MAE).

**2026-06-03 — B36 code-vs-spec rows above are now LANDED in `src/`** (branch `b36-impl`; commits `e300a10`/`f9e72d4`/`1004389` + WS-I working tree). The end-to-end proof is `experiments/test_v14_capstone.py::test_p1_p2_p3_p4_end_to_end_on_bt` (synthetic-BT P1→P2→P3→P4 `fast_dev_run`, green). Per-row status:
- ✅ **B36-per-parcel-pool** (WS-A: block-diagonal one-hot pool, `models/v14_encoder.py`); ✅ **B36-paradigm-b-predictor** + ✅ **B36-parcel-time-mask** (WS-B: `ssl/masked_jepa.py` + `ssl/mask.py` tube/band samplers); ✅ **B36-three-way-latent-valid** (WS-A/B; `v14_encoder.py` + `v14_joint_module.py`); ✅ **B36-encoder-ln-target** (B6: `LN_frame` head removed, loss target = post-`encoder_ln` M4); ✅ **B36-staged-phases** (WS-E: P1/P2 split + ckpt handoff); ✅ **B36-rankme-monitor** (WS-I1: fires from TRAIN step 0 on the post-`encoder_ln` tap). P3 (WS-F) + P4 (WS-G) phases land alongside.
- ⏳ **B36-C5-freq-patch-valid-wiring** — built in the M2 sampler / front-end loss / encoder forward but NOT threaded from `V14JointBrainModule` (WS-H, Ben-gated; deferred by design — BT/capstone is all-valid ⇒ `None` ⇒ exact). Unchanged from the row above.
- ⏳ **B36-mon-mask-002-rewire** — MON-MASK-002 still `shaft_mask`-gated (`v14_joint_module.py:679`); retire-then-replace plan in `reports/b36_mon_mask_002_retire_replace_handoff_2026_06_03.md` (owned by WS-B6/B4/B8). Diagnostic-only (never enters loss/grad); Part 2's band must be re-derived from the first live BT-Lite run, so it cannot fully land offline.
- ⏳ **`check_lock_drift.py` B36 pass** — the guard's superseded-literal set still assumes the soft-bias path exists; adding the B36 forbidden literals (λ_anat / `ln_frame` head / `log(support` soft-routing / inert 2-term-SSL) plus matching exemption markers is **blocked on explicit authorization to edit `.claude/hooks/check_lock_drift.py`** (agent-controlled hook; the auto-mode classifier declines the edit without it). The 4 rules + markers are drafted; surface to Ben.

**2026-05-29 stale-scaffold + doc-rot cleanup sweep** (follows the 5/28 code-vs-lock audit "the code is stale 5/22 scaffold, not the current lock"): deleted 4 dead-scaffold modules + their tests (`experiments/wrs_sampler.py`, `experiments/v14_data.py`, and the 4-/5-term loss-composer accessors `compose_v14_joint_loss` / `v14_joint_loss_coefficients` / `v14_phase_loss_coefficients` + the tests ratifying their superseded targets); deleted the dead `PHASE_SCOPE` ClassVars on the SWEC / AJILE12 / D-cohort studies; deleted the never-read encoder LN heads (`ln_mid`/`ln_frame`/`ln_utt` now live only on `_V14StudentBundle`) and gated `LearnableSubSlotEmbed` behind `m_sub_slots > 1` (None at the B29 M=1 default); fixed stale doc-rot across `extractors/view.py` (hop=128/14.7 Hz → 256/8 Hz + 14.7 Hz mislabel note), `ssl/{__init__,recon,ema,dkoleo}.py` (Phase-1/Phase-2 → joint; `compose.py` → `total_loss.py`+`aggregator.py`; 4-term → B31 2-term; "all 320 slots" → 80), `extractors/parcels_supervised.py` + `monitors/subject_id_leakage.py` (`parcels_supervised[subject]` → B30 `latent_valid`), and the cross-attn `{0,3}` / `M=4` / `4-term`-default comments in `models/v14_encoder.py` + `experiments/{v14_joint,dispatch_v14}.py`. **Mechanical recurrence guard added**: `.claude/hooks/check_lock_drift.py` (PreToolUse on Write/Edit/MultiEdit/NotebookEdit to `src/speech_decoding/**.py`) blocks re-introduction of superseded literals (320-slot/M=4/cross-attn-{0,3}/14.7 Hz/`parcels_supervised[subject]`/`compose.py`), each paired with its canonical lock memo, with sister-cell + historical-framing + `# drift-ok` exemptions; `--scan-tree` runs a full-tree audit (currently clean across 166 files).**

**Pre-Phase-1 critical path** (binding gates):
- **20-row** **code-vs-spec drift** (table below) — B20 patches/hop/freq-embed, B03 shaft-mask, B03b/f `parcels_supervised` (retired by B30 anatomy-gated default — single source of truth via `latent_valid`), per-corpus mains notch, **B27 pure-L1 default + EMA τ=0.999 fixed (no ramp) + teacher full-input contract**, **REF-01 per-clip ref draw + 3-cell operator**, **REF-02 `ref_embed` conditioning**, **B28-DKoleo demote default → 3 sisters + MON-SLOT-REDUNDANCY**, **B28-cross-attn count 2 @ {0,3} → 1 @ {0}**, **B28-anatomy-bias linear warmup**, **B29-joint-collapse P1/P2 → single phase**, **B29-subtype-embed**, **B29-ref-embed**, **B29-no-gating (Item 12, SUPERSEDED 5/28 by B30 — marked-superseded for provenance)**, **B29-monitors (MON-SENSOR-TYPE-CANARY + MON-REF-TYPE-CANARY + MON-HEAD-BALANCE-005 demoted)**, **B29-m1-default (Item 13: drop SubSlotEmbed + K_total 320 → 80 + MON-SLOT-REDUNDANCY threshold rescale)**, **B29-d-bump-384-sister (Item 15: P0 must-run config override at d=384)**, **B29-no-moe-default (Item 14: dense FFN preserved, R-moe-ffn-soft-4 P2 sister module deferred)**, **B30-anatomy-gated-via-latent-valid** (supersedes B29-no-gating; single source of truth via `latent_valid = support.sum(electrodes) > 0`; same rule across all subjects; SWEC trivially all-False → front-end only), **B30-sa-bidirectional-mask** (inactive slots fully bypass latent SA — neither keys nor queries; `attn_mask (L, L)` replaces `key_padding_mask`), **B30-loss-rename-slot-mask** (`slot_mask` → `latent_valid` rename in `slot_loss.py` / `utterance_loss.py` — single name across encoder + losses), **B30-pma-zero-active-guard** (skip PMA + L_post_utterance for clips with `latent_valid.sum() == 0`) not yet in `src/`
- **Code scaffolding**: NT01, NT02, DP01, DP03
- **Pre-dispatch tests**: TST01, TST03, TST05, TST10 + RT10 (`strict=True` at phase boundaries)
- **B18 closure-gate audit** (META — runs after all other open BIG/MEDIUM are ✅; blocks Phase-1 dispatch)

**Recently closed (2026-05-26 12-decision sweep)**: M02, M05, M07, S06, M08, M17, M18, M19, IM11, B17, VIS13, BP20, TIME07.

**Prior closures**: 2026-05-25 PM B03+B12 mask-discipline bundle (b/c/d/f); 2026-05-25 B05/B06/B09/B13/B15; 2026-05-24 B19/B20 (loss + invisible-frontend); 2026-05-23 B01/B02/B08/B11/EX03/EX09/M06/B14/HB01/HB02; full ✅-retained ledger inlined per-entry below.

**Audit provenance**: 244 enumerated gaps across 19 lenses in 5 waves (W1 pre-5/22 → W5 5/23). Closed 2026-05-23 by convergence-driven termination (technical-design yield saturated; process lenses dominated W5). 84 items repatriated 5/23 PM after over-aggressive 88% line-reduction. Audit trail: `docs/archive/v14_blockers_audit_trail_2026_05_22.md`. Closing report (original top-30 + 6-bundle walkthrough order): `v14_blockers_closing_report.md`.

**Severity legend.**
- **BIG** — load-bearing; first-pass dispatch blocks on this.
- **MEDIUM** — has a reasonable default but should be explicitly stated; first-pass survives a generic choice.
- **SMALL** — implementation default; low risk if standard.

## Code-vs-spec gaps (audit 2026-05-25 PM; B25 row added 2026-05-27 AM, rewritten as B26 PM same-day after V-JEPA 2/2.1 PDF re-read; REWRITTEN AGAIN as B27 PM-late after V-JEPA 2.1 Tables 1+2 PDF re-read showed context loss costs ~10pp on clip-level SSv2; REF-01/REF-02 rows added 2026-05-27 PM; **B28-DKoleo / B28-cross-attn / B28-anatomy-warmup rows added 2026-05-27 PM-late**; **B29 rows added 2026-05-28** (joint-collapse, subtype-embed, ref-embed, no-gating, monitors, m1-default, d-bump-384-sister, no-moe-default); **B30 rows added 2026-05-28** (anatomy-gated-via-latent-valid SUPERSEDES B29-no-gating; sa-bidirectional-mask; loss-rename-slot-mask; pma-zero-active-guard); **B31 row added 2026-05-28 PM** (V-JEPA-2-canonical 4-term → 2-term default collapse + PMA-trained-only-at-P3) — must close before Phase-1 dispatch)

Several locked specs have not yet landed in `src/`. Auditing `extractors/view.py`, `models/v14_encoder.py`, and `experiments/dispatch_v14.py` against the current MEMORY.md status block found six divergences (5/25); B25 (5/27 AM) added a seventh; B26 (5/27 PM) superseded B25 in place on the same row with a tighter spec; **B27 (5/27 PM-late) supersedes B26's context-loss + warmup components — drops them from the default while keeping B26's pure-L1 + fixed-EMA + full-input-teacher decisions**; REF-01/REF-02 (5/27 PM ref-aug lock) add two more; **B28 (5/27 PM-late) adds three more rows — DKoleo demote default → sisters, cross-attn count 2 → 1, anatomy-bias linear warmup**; B29 (5/27 PM-late + 5/28) adds eight rows for the joint-default + Item 11/12/13/14/15 cascade; **B30 (5/28) adds four rows and supersedes B29-no-gating in place** — for a total of **twenty** code-vs-spec gaps. Each blocks the corresponding contract; together they block Phase-1 dispatch (none are silently safe to ship).

| Spec line (MEMORY.md / lock memo) | Code state (2026-05-25 PM) | Where to fix |
|---|---|---|
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -nE 'class _PatchStem|F_p=10' src/speech_decoding/models/v14_encoder.py` returns class at line 271 + F_p=10 docstring at line 31] — B20 v4 invisible-frontend lock: **Conv2d (3, 2) patches** at A1, **F_p=10** per-patch freq embed | `models/v14_encoder.py:24-29` uses **per-cell Linear(1 → d) over each (electrode, t, f) scalar** + categorical freq embedding sized to **F=30**, not F_p=10. No Conv2d patcher. | `models/v14_encoder.py` token-block stem; `models/__init__.py` Conv2dPatcher + freq embed (10 vec) |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -n 'hop_length: int = 256' src/speech_decoding/extractors/view.py` returns line 391] — B20: **common hop = 256 → 8 Hz** frame rate | `extractors/view.py:385` `hop_length: int = 128` → **16 Hz** frame rate. T per 1 s = 17 frames (per docstring math). | `MultiStftView.hop_length = 256`; update T_p math in encoder + tests; rerun any baked smoke fixtures |
| MEMORY.md status: "+ comb@60" inline with arch features | **No model-side comb embedding exists.** `v14_encoder.py` has no comb-related symbol. Confirmed not real — was preproc-notch shorthand mis-placed in the arch line. | MEMORY.md status already cleaned (this PM); no code action |
| B03b/B03f: **`parcels_supervised[subject]`** gate for latent SA + loss-side supervision | `v14_encoder.py:188-191` has only `valid_mask: (B, C_max) bool` generic key_padding mask. No `parcels_supervised` extractor; `valid_parcels[clip]` was the prior gate (per B19, JEPA-inverted in P2). | New `extractors/parcels_supervised.py` per B03f spec; rewire `experiments/v14_phase{1,2}.py` loss sites; B03b TST-MASK-002 |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -nE 'class BTShaftMaskExtractor' src/speech_decoding/extractors/shaft_mask.py` + `grep -n 'shaft_mask.*BTShaftMaskExtractor' src/speech_decoding/experiments/dispatch_v14.py` returns wiring under `joint_phase` guard; tests `test_b23_dispatch_joint_phase_wires_shaft_mask_extractor` + `test_b23_dispatch_supervised_phase_4_omits_shaft_mask` in `experiments/test_v14_dispatch_wired.py`; routing tests `test_v14_joint_brain_module_routes_shaft_mask_student_only` + `test_v14_joint_brain_module_step_accepts_shaft_mask_in_batch` in `experiments/test_v14_joint_module.py`; 8 BT wrapper tests in `extractors/test_shaft_mask.py`] — B03: **shaft-block electrode mask = DROP via C_MAX key_padding_mask** (joint SSL phase only) — **K-cap revised twice 2026-05-27**: AM K=3 → K ≤ 2 fraction-based; **PM K ≤ 2 → K=1 default**: `K = 1 if N_shafts ≥ 2 else 0` with block α only (shaft-extent 0.45–0.60 × time-extent 0.15–0.30). NeuralFetch-style wrapper `BTShaftMaskExtractor(BaseStatic)` composes BT subject anatomy (`depth-wm.csv`) → `SubjectAnatomy` (shaft ids from `parse_shaft` of electrode label prefixes) → `ShaftMaskExtractor.__call__` → pad to `(c_max,)` bool with `False` for padding slots. Wired into `dispatch_v14.py` segmenter under `"shaft_mask"` key **only when `joint_phase=True`** (supervised Phase-4 path stays unaffected — confirmed by `test_b23_dispatch_supervised_phase_4_omits_shaft_mask`). `V14JointBrainModule._extract_student_kwargs` routes `batch.data["shaft_mask"]` STUDENT-ONLY (B26 teacher full-input contract); the teacher forward reuses the kwargs dict without it. Sisters `R-shaft-K2` / `R-shaft-K3-mixed-3block` / `R-shaft-K1-explicit` reach the inner `ShaftMaskExtractor` through `k_override` + `extent_blocks` constructor kwargs on `BTShaftMaskExtractor` (sister dispatch fields are a future surface bump if a sweep cell needs them). |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -nE 'MAINS_NOTCH_BY_CORPUS\|notch_filter_hz_by_corpus' src/speech_decoding/experiments/dispatch_v14.py` returns the constant at line 95 and the dispatch field at line 229 with build-time corpus-mix → notch-map cross-check at lines 305–328; tests `test_b28_dispatch_swec_corpus_uses_50hz_notch`, `test_b28_dispatch_mains_notch_kwarg_overrides_default`, `test_c3_dispatch_rejects_corpus_mix_with_missing_notch_entry`, `test_c3_dispatch_per_corpus_notch_routes_into_bt_extractor`, `test_b28_cli_mains_notch_accepts_swec_50` in `experiments/test_v14_dispatch_wired.py`] — **per-corpus mains notch** (60 Hz US, 50 Hz SWEC). `dispatch_v14.py` lifts a `MAINS_NOTCH_BY_CORPUS: dict[str, float]` constant (`braintreebank=60.0`, `swec_ieeg=50.0`, `d_cohort=60.0`, `ajile12=60.0`); `notch_filter_hz_by_corpus` field on `build_v14_experiment` accepts an explicit per-corpus map and falls back to the constant. The corpus-mix loop asserts every corpus has a notch entry → `missing_notch` raises `ValueError`. SWEC dispatch passes `notch_filter_hz_by_corpus={"swec_ieeg": 50.0, ...}` and the SWEC extractor receives 50 Hz, BT/D/AJILE12 receive 60 Hz. CLI `--mains-notch` validates 50/60 acceptance. |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -nE 'loss_form.*Literal.*"l1"' src/speech_decoding/ssl/recon.py` returns line 42 with `"l1"` first in default tuple] — **B27** (2026-05-27 PM-late, PARTIAL REVERT of B25 + B26 context-loss components): **pure L1** default across all 4 SSL prediction terms (L_pre_frame_masked, L_mid_slot, L_post_frame, L_post_utterance) — **KEPT from B26** + **EMA τ=0.999 FIXED, no ramp** (V-JEPA 2 §2.4 explicit) — **KEPT from B26** + **teacher full-input contract locked** — **KEPT from B26** + **NO L_pre_frame_context term** (B27 DROP — V-JEPA 2.1 §2.3.1 Eq 2 companion removed: their Table 1+2 show context loss costs ~10pp on clip-level SSv2 (72.8 → 62.5 at best-case λ=0.5+weighted+warmup); only Deep Self-Supervision recovers, and v14 can't replicate DSS because M2/M3/M4 live in different token spaces. v14's downstream is clip-level only.) + **NO λ_ctx warmup schedule** (B27 DROP — no context loss to weight). Sister `R-context-loss-vjepa21-recipe` (P1, single cell) reinstates full V-JEPA 2.1 recipe as iEEG-side falsification cell. P3 distillation stays Smooth-L1 (unchanged). | `ssl/recon.py:42` has `loss_form: tp.Literal["mse","smooth_l1"] = "mse"` — needs new `"l1"` branch using `nn.L1Loss(reduction='none')`; default flip target is `"l1"`. Per-term losses (L_pre_frame_masked / L_mid_slot / L_post_frame / L_post_utterance) NOT yet wired in `src/` at all; only the foundational `recon_loss` primitive exists. **B26's planned `ssl/context_loss.py` module + λ_ctx warmup schedule are SCOPED OUT of the default critical path by B27**; they only need to exist if `R-context-loss-vjepa21-recipe` sister is dispatched. `ssl/distill.py:39` Smooth-L1 default unchanged (P3, NOT in scope). EMA momentum ramp + teacher-input scope not yet locked in `experiments/v14_phase{1,2}.py`. | (1) Add `"l1"` branch to `ssl/recon.py:42`; flip default → `"l1"`. (2) Wire per-term losses in `experiments/v14_phase{1,2}.py` against L1 default; expose `loss_form` dispatch field for R-l2-loss / R-smoothl1-beta sisters. (3) Set EMA momentum to fixed τ=0.999 (remove the `0.99 → 0.9999 ramp` from `experiments/v14_phase{1,2}.py`). (4) Lock teacher forward to FULL-input contract (no patch mask, no shaft mask on teacher; only per-corpus valid-bin mask) — add runtime assert in debug builds. (5) Leave `ssl/distill.py:39` Smooth-L1 default unchanged — P3 cross-modal regression is out of B27 scope. (6) DEFERRED to sister-dispatch path only: `ssl/context_loss.py` (Chebyshev d_min on per-electrode F-patch × T-patch grid + tests) + λ_ctx linear warmup schedule wiring — gated behind `R-context-loss-vjepa21-recipe` sister flag, not on the default critical path. The B26 plan for these two items is preserved in [[project_v14_loss_design_amendment_b26_2026_05_27]] §"Code work items" if/when the sister is dispatched. |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -nE 'class RefAugMultiStftView\|class RefIdxExtractor\|def draw_ref_idx' src/speech_decoding/extractors/ref_aug.py` returns 3 hits; `grep -n 'RefAugMultiStftView\|RefIdxExtractor' src/speech_decoding/experiments/dispatch_v14.py` shows the dispatch construction (lines 38, 353, 408); tests `test_b_bug_4_default_log_stft_view_pins_ref_idx_to_shaft_car` + ref_aug suite under `extractors/test_ref_aug.py`] — **REF-01** (2026-05-27 PM): **per-clip ref draw + 3-cell ref operator** in dataloader — uniform over `{shaftCAR, bipolar, Laplacian}` PRE-Multi-STFT; bipolar drops 1 channel per shaft; SWEC degenerates to global-CAR; per-clip seed `hash(subject, session, clip_idx)`. `extractors/ref_aug.py` provides `RefAugMultiStftView(MultiStftView)` (per-clip uniform draw over `ref_modes`, applies the matching operator pre-Multi-STFT, pads to C_MAX), `RefIdxExtractor(CARIeegExtractor)` (per-clip `ref_idx` scalar via `draw_ref_idx(event, start, duration, seed, ref_modes)` using SHA-256 over the timeline + start + duration), `REF_MODES = ("shaft_car", "bipolar", "laplacian")` + `REF_MODE_TO_IDX`. Dispatch builds the view with the dispatch seed; the segmenter `extractors["ref_idx"] = RefIdxExtractor(seed=ref_seed_for_label, ref_modes=ref_modes_for_label)` shares the seed with the operator so the per-clip draw stays aligned across `electrode_tokens` and the `ref_idx` token. Sister `R-no-ref-aug` retired (REF-01 IS the default; the fixed-shaftCAR falsifier is reached by constructing a `LogStftView(car="shaft")` for `electrode_tokens_extractor` and the dispatch collapses to a single `ref_modes=("shaft_car",)` label). |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -n 'ref_embed = nn.Embedding(3' src/speech_decoding/models/v14_encoder.py` returns line 740; `grep -n 'ref_embed_enabled\|ref_embed_reuse_kv' src/speech_decoding/experiments/dispatch_v14.py` shows the field at lines 220–221 + CLI flags at lines 628 / 634; encoder forward broadcasts `ref_embed(ref_idx)` into `cond_emb` at line 905 and reuses it in cross-attn K/V at line 1058 when `ref_embed_reuse_kv=True`] — **REF-02** (2026-05-27 PM, default ON via B29 Item 11): **`ref_embed: (3, d=256)` conditioning** additive at A1 patch-embed output, broadcast across `(C, F_p, T_p)` then re-added into cross-attn K/V when `ref_embed_reuse_kv=True` (default). Construction gated on `ref_embed_enabled=True` (default); when off, `self.ref_embed = None` and the conditioning path skips the term. `ref_idx: (B,) long` flows from the `RefIdxExtractor` (REF-01) → `_extract_student_kwargs` strips trailing singletons → encoder forward indexes `self.ref_embed`. Sister `R-no-ref-embed` is the `--no-ref-embed` CLI flip (`ref_embed_enabled=False`). |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -nE 'l_dkoleo_m4.*Optional.*None' src/speech_decoding/ssl/total_loss.py` shows kwarg default None; 4-term composer omits DKoleo] — **B28-DKoleo** (2026-05-27 PM-late): **DKoleo demoted from default to sister-only** — drop `0.1 · L_DKoleo@M4` from the P1+P2 4-term objective; route through MON-SLOT-REDUNDANCY monitor (every 10k steps, per-clip 320-slot AND per-batch CLS-analog cosine probes) which gates dispatch of three sister cells: `R-dkoleo-batch-cls-unit` (DINOv2/v3-faithful per-batch CLS-analog), `R-dkoleo-intra-clip-slots` (B21 default per-clip 320 slots), `R-vicreg-slot-variance` (VICReg variance hinge per slot dim). | `ssl/koleo.py` exists per B21 default-bundled wiring (assumed unchanged) but B28 demotes it: not yet rewired off the default loss path in `experiments/v14_phase{1,2}.py`. MON-SLOT-REDUNDANCY monitor + per-sister loss arms not yet present. | (1) Remove `+ 0.1 · L_DKoleo` term from default `L_total` in `experiments/v14_phase{1,2}.py`. (2) Add MON-SLOT-REDUNDANCY logger: every 10k steps on held-out 256-clip probe batch, log `per_clip_cos.off-diag.{mean,max,pct95}` over `slots_M4.mean(dim=t_p)` + `batch_cos.off-diag.{mean,max,pct95}` over `L_post_utterance_clip_vec` (B, d). (3) Add sister-cell flags `dkoleo_variant: tp.Literal["off","batch_cls_unit","intra_clip_slots","vicreg_slot_variance"]` to dispatch + thresh-gated escalation logic. (4) Per-sister loss modules: `ssl/koleo_batch_cls.py` (per-batch on (B, d) utterance-pooled CLS-analog with coeff 0.1, optional batch sub-sample to 16-32 to match DINOv2 regime); `ssl/koleo_intra_clip.py` (per-clip 320-slot operator, B21 unchanged, coeff 0.1); `ssl/vicreg_slot_variance.py` (per-dim variance hinge `L_var = mean_d(max(0, 1 − std_{p ∈ 320}(slots[:, p, d])))`, coeff TBD via 1-GPU-h BT-Lite preflight over {1, 5, 10, 25}). **B22 reactive `L_DKoleo@M3` arm**: route through the chosen DKoleo variant when armed; mechanism not on default path. |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -n 'cross_attn_positions' src/speech_decoding/models/v14_encoder.py` returns default `[0]` per Perceiver-IO canonical (Jaegle 2021)] — **B28-cross-attn** (2026-05-27 PM-late): **1 encoder cross-attn @ layer 0 of the latent stack** (was 2 cross-attns @ {0, 3} in v3); the second cross-attn at position 3 is dropped per Perceiver-IO standard pattern (Jaegle 2021 arXiv:2107.14795: encode-process-decode = 1 input cross-attn + L untied self-attns + 1 output cross-attn; the appendix explicitly drops repeated cross-attends as "not worth the 303B FLOPs"). M3 = post the single cross-attn @ layer 0 / pre self-attn-0 (B22 supervision point shifts from "post cross-attn-1, pre self-attn-0" to "post cross-attn ❺, pre self-attn-0" — semantically identical since cross-attn ❺ IS the only one now). | `v14_encoder.py` (assumed v3 wiring per status header) — 2 cross-attns at positions 0 and 3 of the latent stack, latent ❻ split as 3+3 around the second cross-attn. No `n_cross_attn` dispatch field. | (1) Remove the second cross-attn at position 3 in `v14_encoder.py`; collapse ❻ to contiguous L=6 self-attns downstream of the single cross-attn ❺ @ layer 0. (2) Update param-count assertions in `models/test_v14_encoder.py` from ~15.024M → ~14.235M trainable (subtract ~0.79M cross-attn projection weights). (3) Add `n_cross_attn: int = 1` dispatch field on the encoder for sister `R-perceiver-original-2-cross-attns` (n=2 @ {0, 3}) and the existing P1 sister `R-perceiver-3-cross-attns-{0,2,4}` (n=3 @ {0, 2, 4}). (4) Update Monitor F1 / cross-attn entropy log to only reference the single cross-attn ❺ (was logged separately for cross-attn-1 and cross-attn-2 in v3). |
| **✅ CLOSED 2026-05-28** [SUPERSEDED by B29 per-clip λ_anat gate; step-warmup retained only via `R-with-anatomy-step-warmup` P1 sister] — **B28-anatomy-warmup** (2026-05-27 PM-late): **P1→P2 anatomy bias linear warmup** — replace the B19 discrete bias toggle (`bias_enabled=False` in P1, `bias_enabled=True` in P2 from step 0) with a scheduled scalar `λ_anat(step) = 0` for `step < 0.75·N_P1`, linear ramp `0 → 1` over `(0.75·N_P1, N_P1 + 0.25·N_P2)`, `λ_anat(step) = 1` for `step ≥ N_P1 + 0.25·N_P2`. Scale ❺'s `log(support[e,p] + ε)` bias by this scheduled scalar. **SUPERSEDED BY B29 per-clip gate**: keep `λ_anat: nn.Buffer` plumbing (the registered-buffer mechanism is reused) but the schedule becomes per-clip gate (`λ_anat = 1` for anatomy-rich clips, OFF for SWEC) — no step warmup. `R-with-anatomy-step-warmup` P1 sister reinstates the B28 step schedule. | `v14_encoder.py` (assumed v3 wiring) treats the bias as on/off via `bias_enabled: bool` buffer. No `λ_anat` schedule, no step-conditional bias scaling in the cross-attn forward. | (1) Add `λ_anat: nn.Buffer` (registered scalar) on the cross-attn module in `v14_encoder.py`; replace `if bias_enabled: logits += bias` with `logits += λ_anat * bias`. (2) **REVISED FOR B29**: drive `λ_anat` from per-clip metadata (1.0 for anatomy-rich clips, 0.0 for SWEC clips) via dataloader-set scalar, not from a global step schedule. (3) `AnatomyBiasWarmupScheduler` callback DEFERRED to `R-with-anatomy-step-warmup` sister only. (4) Sister flag `anatomy_bias_mode: tp.Literal["per_clip_gate_b29","warmup_b28","step_b19","on_from_p1"]` with default `per_clip_gate_b29`. (5) Unit test: per-clip `λ_anat ∈ {0, 1}` propagates through forward; anatomy-rich clip gets bias, SWEC clip does not. |
| **✅ CLOSED 2026-05-28** [Verifiable: `experiments/v14_joint.py` (`V14JointExperiment(V14Experiment)` pinned to `JOINT_PHASE_VALUE=1` with `_build_brain_module` constructing `V14JointBrainModule`), `experiments/v14_joint_module.py::V14JointBrainModule` (EMA τ=0.999 fixed via `fixed_ema_schedule`; 4-term L_total via `compute_v14_ssl_losses`; predictor trained from step 0, no warm-start), `dispatch_v14.py` `--phase 1` builds the joint experiment via `phase_mode="joint_b29"` ClassVar plumbed onto `V14ParcelPerceiver`. Tests: `test_v14_joint.py::test_v14_joint_phase_literal_is_joint_b29` + `test_v14_joint_*_rejects_*` (15 tests), `test_v14_joint_module.py` (9 tests covering 4-term breakdown, EMA τ=0.999, teacher full-input contract, predictor-None fallback L1 form), `test_dispatch_v14_phase_flag.py::test_phase_1_build_brain_module_uses_joint_ssl_module`] — **B29-joint-collapse** (B2.1 + B2.2 runtime arm landed). Sister `R-keep-phase-split` continues to dispatch through `V14Experiment` directly with explicit `phase=1` then `phase=2`. |
| **✅ CLOSED 2026-05-28** [Verifiable: default path skips `subtype_embed` per B29 Item 11 flip + audit; sister gating mechanism documented (M3AE 2022 §3.1 precedent), no encoder-side default code change required] — **B29-subtype-embed** (2026-05-27 PM-late, Item 2; **citation cleanup + default-OFF flip 2026-05-28 PM Ben call**): **2-entry `{sEEG-depth, ECoG}` `subtype_embed: (2, d=256)` (512p) additive at A1** + reused in cross-attn K/V. **Default OFF** (flipped from prior default-ON per DIVER-1 §4.1 in-domain evidence — see [[project_v14_subtype_embed_precedent_audit_2026_05_28]]). Per-electrode binary field carried in batch as `subtype_idx: (B, C_max) long`. Sisters: **`R-subtype-embed-input-only` P0** (M3AE-faithful, no K/V reuse) + **`R-subtype-embed-on-with-kv-reuse` P0 NEW** (prior default flipped to sister) + `R-subtype-embed-3way` P2-if-budget. **Citation:** primary precedent = M3AE 2022 §3.1, NOT MultiMAE; analog = BrainOmni 2025 §2.2 + DIVER-1 2025 §2.1; LaBraM/Brant-2 NOT precedents. **Expected:** DIVER-1 §4.1 finds the mechanism net-neutral on iEEG. | `v14_encoder.py` has no `subtype_embed` parameter; `subtype_idx` is not propagated through the batch. Per-corpus subtype assignment (BT/D/AJILE12-depth = `sEEG-depth`, AJILE12-ECoG = `ECoG`) not wired in dataloader. | (1) Default path: do NOT add `subtype_embed` to `models/v14_encoder.py`. No-op on default; `subtype_idx` propagation not needed unless sister dispatched. (2) For sister cells: add `subtype_embed = nn.Embedding(2, d)` gated on `subtype_embed_enabled=True`; broadcast across `(F_p, T_p)` per electrode + add to A1 output; conditionally reuse in cross-attn K/V gated on `subtype_embed_reuse_kv=True`. (3) Add `subtype_idx: (B, C_max) long` to batch contract WHEN sister dispatched. (4) Per-corpus subtype assignment in manifests (lazy: only when sister runs). (5) Dispatch fields: `subtype_embed_enabled: bool = False` (FLIPPED 5/28 PM Ben call), `subtype_embed_reuse_kv: bool = True` (effective only when `subtype_embed_enabled=True`), `subtype_embed_vocab: tp.Literal["binary","three_way"] = "binary"`. |
| **B29-ref-embed** (2026-05-27 PM-late, Item 11): **3-entry `{shaftCAR, bipolar, Laplacian}` `ref_embed: (3, d=256)` (768p) additive at A1** + reused in cross-attn K/V. Promoted optional → default ON. **Composes with REF-02** above (REF-02 specified the mechanism; B29 Item 11 flips default-OFF → default-ON). | Same as REF-02 above — no `ref_embed` in `v14_encoder.py`; `ref_idx` not propagated. | (1) Implementation identical to REF-02 (single `nn.Embedding(3, d)` + broadcast + cross-attn K/V reuse). (2) Default flag flip: `ref_cond_enabled: bool = True`. (3) `R-no-ref-embed` P1 sister flag for `ref_cond_enabled=False`. |
| **B29-no-gating** (2026-05-27 PM-late, Item 12, ~~SUPERSEDES B03b/f~~ — **SUPERSEDED 5/28 BY B30 row below**; KEPT FOR PROVENANCE): ~~DROP `parcels_supervised[subject]` gating on L_mid_slot + L_post_frame — both losses fire on all 80 slots for all clips regardless of subject's parcel coverage.~~ B30 anatomy-gated-symmetric (`project_v14_anatomy_gated_symmetric_2026_05_28.md`) replaces this with per-subject anatomy-gated supervision (`latent_valid = support.sum(electrodes) > 0`) applied as the SAME rule across all subjects — SWEC degenerates to all-False (front-end only) without special-case code. Item 12's "all-True everywhere" mechanism is reinstated as the `R-item-12-all-true` P0 falsifier under B30. MON-HEAD-BALANCE-005 demotion KEPT (still health canary, same kill bound `[0.3, 3.0]`). The original B03b/f `parcels_supervised` extractor stays optional (used only by sister `R-item-12-all-true` to construct all-True override; `latent_valid` from support is the default). | n/a (superseded; see B30-anatomy-gated-via-latent-valid row) | n/a (superseded; see B30-anatomy-gated-via-latent-valid row) |
| **B29-monitors** (2026-05-27 PM-late, Item 11 + Item 12 add-ons): **MON-SENSOR-TYPE-CANARY** (per-batch sensor-type linear-probe F1 from M2/M3 every 10k steps, target band `[0.7, 0.95]`) + **MON-REF-TYPE-CANARY** (per-batch ref-type linear-probe F1 from M2/M3 every 10k steps, target band `[0.7, 0.95]`) + **MON-HEAD-BALANCE-005 demoted** (was kill criterion; now health canary). | None of the three monitors are implemented in `experiments/v14_phase{1,2}.py`. | (1) Add linear-probe utilities in `experiments/monitors/sensor_type_canary.py` + `ref_type_canary.py` reading `subtype_idx` + `ref_idx` from the held-out probe batch. (2) Hook both into the 10k-step monitor cadence. (3) MON-HEAD-BALANCE-005 already needs adding (was never built); add with health-canary interpretation, not kill bound. |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -n 'm_sub_slots: int = 1' src/speech_decoding/models/v14_encoder.py` returns lines 508 and 1413 → M=1 default + SubSlot degenerates to ε at K=1] — **B29-m1-default** (2026-05-27 PM-late, Item 13): **M=1 default, 80 slots = 80 DK parcels × 1**. SubSlotEmbed dropped. `z_init[p] = LearnableParcelEmbed[p] + ε` (replaces `z_init[p·M+s] = LearnableParcelEmbed[p] + LearnableSubSlotEmbed[s] + ε`). 16× SA + 4× cross-attn compute reduction. LN_mid (B22) scope 320 → 80. MON-SLOT-REDUNDANCY thresholds (B28) calibrated for 320 slots; needs rescale for 80 (open task — escalation thresholds `per_clip_cos.pct95 > 0.7` may scale with slot count; verify the per-clip-cos statistic distribution at 80 slots before transferring the threshold). `R-m4-slots` P0 falsifier preserves M=4 default. | `v14_encoder.py` (assumed v3 wiring) has both `LearnableParcelEmbed: (80, d)` + `LearnableSubSlotEmbed: (4, d)` + index maps `parcel_of_latent[i] = i // 4` and `sub_slot_of_latent[i] = i % 4`. | (1) Drop `LearnableSubSlotEmbed` from `v14_encoder.py`. (2) Drop `sub_slot_of_latent` index map; `parcel_of_latent[i] = i` becomes identity at M=1. (3) Reduce `K_total = K * M` config from 320 to 80; add `M: int = 1` dispatch field (M=4 sister flips it back). (4) Update param-count assertions in `models/test_v14_encoder.py` — remove ~1024 params (4 × 256 SubSlotEmbed). (5) Update LN_mid / DKoleo monitor probe sites in `experiments/monitors/` to operate over 80 slots not 320. (6) Rescale MON-SLOT-REDUNDANCY escalation thresholds — **pre-preflight proposal landed 2026-05-28** (see B28 memo §MON-SLOT-REDUNDANCY threshold rescale): `per_clip_cos.pct95 > 0.5`, `batch_cos.pct95 > 0.7` (unchanged — invariant under K), `per_clip_cos.diag-zeroed.mean > 0.35`. **1-GPU-h BT-Lite calibration preflight still required to lock empirical thresholds at μ + 3.1·σ** before Phase-1 dispatch; probe-batch bumped 256 → 1024 to compensate for 4× pct95 estimator noise. (7) HB02 re-cost: **CLOSED 2026-05-28 (Agent 1)** — confirmed ~975 H100-h median (range 510–1,900 across MFU 18–40%), ~10% of HB02 ~10k envelope. Item 13 reduction matches prediction (~48% step FLOPs at fixed C̄=108). Item 12's "~5% extra compute" was overstated by ~50–100× — true increment <0.1% of step. |
| **✅ CLOSED 2026-05-28** [No-op on default architecture; promotion-gate evaluation handled at dispatch-cell config-override layer (no encoder/dispatch wiring required pre-sister-launch)] — **B29-d-bump-384-sister** (2026-05-28, Item 15): **R-d-bump-384 P0 must-run sister at d=384** (was R-d-bump-384 P1; PROMOTED). Single-config override — no code change to default architecture. Cell-0 BT-Lite cost ~11–22 H100-h at d=384 vs ~5–10 at d=256 (+124% FLOPs). **Promotion gate**: ≥0.02 AUROC over d=256 default at Cell-0 BT-Lite scale to become v15 default. | `dispatch_v14.py` `d_model: int = 256` is a hard default. Cell-0 sister dispatch needs config override path. | (1) Verify `d_model` is a single dispatch field (not hardcoded in multiple modules). (2) Add Cell-0 sister `R-d-bump-384` config (single field override `d_model=384`). (3) Log promotion-gate evaluation (Cell-0 BT-Lite AUROC delta vs d=256 baseline at matched compute) in dispatch metadata. (4) `R-d-bump-512` P2 if-budget sister: `d_model=512` (+298% FLOPs; breaks HB02 envelope; falsifier-only). |
| **✅ CLOSED 2026-05-28** [No-op on default architecture; Soft MoE deferred behind P0/P1 completion + budget per audit Agent B/C estimates — sister-only follow-up tracked in [[project_v14_moe_ffn_audit_2026_05_28]]] — **B29-no-moe-default** (2026-05-28, Item 14): **Dense FFN preserved across all 6 SA blocks; MoE-FFN NOT in default.** No code change to default architecture. `R-moe-ffn-soft-4` P2 if-budget sister would need a new `src/speech_decoding/models/soft_moe.py` module (Soft MoE per Puigcerver 2024 arXiv:2308.00951; ~2-day implementation per audit Agent B/C estimates) IF the sister is ever dispatched — gated behind P0/P1 completion + calendar budget. | No-op on default. `soft_moe.py` does not exist; not blocking Phase-1 dispatch since not in default. | (1) Do NOT replace `nn.Linear` MLPs in the SA blocks. (2) For `R-moe-ffn-soft-4` sister (P2 if-budget, NOT default): build `soft_moe.py` per Puigcerver 2024 (4 experts soft-mixed, no top-k); replaces dense FFN in latter 3 of 6 SA blocks; needs sister-only dispatch flag `ffn_variant: tp.Literal["dense", "soft_moe_4"] = "dense"`. (3) `§Future Work` mention in paper draft citing 3 HIGH-severity integration risks per the audit memo. |
| **B30-anatomy-gated-via-latent-valid** (2026-05-28, SUPERSEDES B29-no-gating; canonical lock [[project_v14_anatomy_gated_symmetric_2026_05_28]]): **Single source of truth `latent_valid = (support.sum(over electrodes) > 0)` applied as SAME rule across all subjects.** Slot K is active (participates in SA + slot losses) iff subject has ≥1 electrode covering DK parcel K. Eliminates cross-subject asymmetry without forcing slot-K parameters to learn uniform-pool default on no-coverage subjects. SWEC = front-end only (zero anatomy → all-False slot mask → zero gradient on L_mid_slot/L_post_frame/L_post_utterance; L_pre_frame_masked still fires). Sisters: **`R-parcels-supervised-gating` retired** (IS the new default — the old `parcels_supervised` mechanism semantically matches `latent_valid` for BT-with-anatomy, but B30 collapses both into the support-derived single mask); `R-item-12-all-true` (P0 NEW) — passes `torch.ones_like(latent_valid)` to all slot-axis losses (reinstates B29 Item 12 mechanism as falsifier); `R-drop-swec` (P1 NEW) — train without SWEC at all (tests SWEC's marginal value at front-end only); `R-sa-key-only` (P1 NEW) — preserve current key-only mask in SA (falsifier of bidirectional masking — see B30-sa-bidirectional-mask row). **✅ Partial closure 2026-05-28**: orchestrator-layer plumbing lands at `ssl/aggregator.py:compute_v14_ssl_losses`, which computes a single `latent_valid` tensor per batch and threads the SAME object to `L_mid_slot`, `L_post_frame`, `pma_student`, `pma_teacher`, with `clip_valid` (= `latent_valid.any(dim=1)`) flowing into `utterance_mse_loss`. Unit-tested at `ssl/test_aggregator.py` (6 tests: B30 single-source-of-truth invariance, SWEC zero-contribution to the 3 B30-gated terms, all-SWEC batch finiteness, gradient flow only to student). The Lightning `_run_step` wrapper that calls this aggregator is the remaining piece (gated on SCAFFOLD-02 V14Data). | Loss primitives `slot_loss.masked_mse_slot_time` + `utterance_loss.pma_then_mean` use `latent_valid` parameter (renamed under B30-loss-rename-slot-mask row below); **orchestrator landed**: `ssl/aggregator.py:compute_v14_ssl_losses` (2026-05-28). Lightning `training_step` wrapper still pending SCAFFOLD-02. `extractors/parcels_supervised.py` retained for sister `R-parcels-supervised-gating`-style mechanisms (now redundant with `latent_valid` for BT). | (1) Training Experiment (when written) computes `latent_valid = (support.sum(dim=1) > 0)` once per batch and passes to BOTH the encoder forward (latent SA attn_mask construction — see B30-sa-bidirectional-mask) AND every slot-axis loss primitive (`masked_mse_slot_time(latent_valid=...)`, `pma_then_mean(latent_valid=...)`). (2) Assert: same `latent_valid` tensor object is used across L_mid_slot + L_post_frame + L_post_utterance (catches accidental mask-mismatch silent bugs). (3) For sister `R-item-12-all-true`: dispatch field `latent_valid_override: tp.Literal["support", "all_true", "parcels_supervised"] = "support"`; sister sets `"all_true"` → loss aggregator passes `torch.ones_like(latent_valid)`. (4) For sister `R-parcels-supervised-gating`: sister sets `"parcels_supervised"` → loss aggregator uses the old `extractors/parcels_supervised.py` output instead of `support`-derived `latent_valid` (differs from B30 default only on SWEC: pre-B29 expansion to all-True for SWEC under that extractor vs B30's all-False from support). |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -n 'attn_mask: Optional\[Tensor\]' src/speech_decoding/models/v14_encoder.py` returns `_PlainMultiHeadSelfAttention.forward` accepting `(B,L,L)` bidirectional mask; `parcel_attn_mask = latent_valid.unsqueeze(2) & latent_valid.unsqueeze(1)` at `_LatentSelfAttnBlock.forward` line ~386; 2 new tests in `test_v14_latent_self_attn_mask.py` (invalid-query bypass + zero-active no-NaN)] — **B30-sa-bidirectional-mask** (2026-05-28, canonical lock [[project_v14_anatomy_gated_symmetric_2026_05_28]]): **Inactive slots fully bypass latent SA — neither keys nor queries.** Replace `key_padding_mask = ~latent_valid` (current key-only behavior; per B03b's pre-B30 spec) with `attn_mask (L, L)` constructed as `latent_valid[:, :, None] & latent_valid[:, None, :]` per batch element. Inactive-slot rows AND columns are masked. Inactive slots pass through unchanged (residual only). Removes the conflicting gradient on shared slot embedding parameters from no-coverage subjects updating slot K via SA pulling from other slots. Sister `R-sa-key-only` (P1) preserves the current key-only mask as falsifier. **Implementation choice**: `attn_mask (L, L)` (correctness first; no compute savings — full O(L²) attention still computed, then masked). Gather/scatter (real O(L_active²) speedup, especially on SWEC where SA is fully bypassed) deferred as perf-only follow-up — open a separate row if SA dominates step time after first profile. | `v14_encoder.py` latent SA (assumed wiring per B03b: `latent_self_attn.key_padding_mask = ~supervised_slot_mask`). Today's `key_padding_mask` masks inactive slots as keys only; they still attend OUT to other slots as queries. | (1) In `v14_encoder.py` latent SA forward, replace `key_padding_mask=~latent_valid` with `attn_mask=latent_valid[:, :, None] & latent_valid[:, None, :]` (shape `(B, L, L)`, bool, True = attend). (2) Update `models/test_v14_latent_self_attn_mask.py` to assert bidirectional masking: perturbation in inactive-slot input has zero effect on active-slot output (existing direction) AND inactive-slot output is unchanged when active-slot inputs are perturbed (new direction). (3) Sister `R-sa-key-only` dispatch field `sa_mask_mode: tp.Literal["bidirectional", "key_only"] = "bidirectional"`. (4) Gather/scatter perf-follow-up: open new blocker row only if profiling shows SA > 30% of step time (likely no — cross-attn @ layer 0 + L_pre_frame_masked dominate). |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -nE 'latent_valid: Tensor' src/speech_decoding/ssl/slot_loss.py src/speech_decoding/ssl/utterance_loss.py` returns slot_loss.py:51 + utterance_loss.py:119; zero `\bslot_mask\b` remaining in active ssl/ source per grep] — **B30-loss-rename-slot-mask** (2026-05-28, canonical lock [[project_v14_anatomy_gated_symmetric_2026_05_28]]): **Rename `slot_mask` parameter → `latent_valid` in `ssl/slot_loss.py:masked_mse_slot_time` and `ssl/utterance_loss.py:pma_then_mean`.** Single name across encoder + losses; eliminates the three-mask conflation trap (`support`-derived `parcel_covered` / `latent_valid` / `parcels_supervised` all reaching the same parameter via different semantic claims). Docstrings updated to state "derived from `support.sum(electrodes) > 0`; SWEC degenerates to all-False naturally; sisters override via `latent_valid_override` dispatch field, not by passing different masks here." No back-compat alias — early stage, no external callers. Tests updated to use `latent_valid=` kwarg. | `ssl/slot_loss.py:34` uses `slot_mask: Tensor` parameter; `ssl/utterance_loss.py:75` uses `slot_mask: Tensor` parameter that gets passed as `pma(m4, latent_valid=slot_mask)` (the docstring-vs-encoder-convention mismatch flagged in B30 lock memo §"The silent-default trap"). Test files `test_slot_loss.py` + `test_utterance_loss.py` use `slot_mask=` keyword everywhere. | (1) Rename parameter `slot_mask` → `latent_valid` in `masked_mse_slot_time` signature + body + ValueError / TypeError messages. (2) Same in `pma_then_mean`. (3) Update both files' docstrings to state the support-derived single-source-of-truth convention (drop references to `parcels_supervised` from the primitive docstrings — the primitive doesn't care about the upstream policy, it just consumes a boolean validity mask). (4) Update `test_slot_loss.py` (all `slot_mask=mask` → `latent_valid=mask`; comments / docstring referring to `parcels_supervised` updated to `latent_valid`). (5) Same in `test_utterance_loss.py`. (6) Confirm zero other src/ callers (grep `\bslot_mask\b` in `src/speech_decoding/`; only B03b/f code-state references in `parcels_supervised.py` and `subtype_meta.py` comments are legitimate — those describe pre-B30 history). |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -nE 'latent_valid_override\|sa_mask_mode' src/speech_decoding/experiments/dispatch_v14.py src/speech_decoding/experiments/v14_joint.py` returns hits in both files; 3 new tests `test_v14_joint_experiment_{accepts_b30_sister_flag_defaults,rejects_b30_sister_latent_valid_override,rejects_b30_sister_sa_mask_mode}` in `experiments/test_v14_joint.py`] — **B30-dispatch-sister-flags** (closed dispatch-surface scope; sister RUNTIME branches stay open under #97 B2.2 / encoder follow-up). `V14JointExperiment` carries `latent_valid_override: Literal["support", "all_true", "parcels_supervised"] = "support"` + `sa_mask_mode: Literal["bidirectional", "key_only"] = "bidirectional"`. Default values accept (recorded in run-record YAML); sister values raise `NotImplementedError` at construction with explicit pointers to #97 B2.2 / encoder latent-SA key-only branch. Dispatch CLI exposes `--latent-valid-override` + `--sa-mask-mode`; supervised Phase-4 path rejects non-default flag values with a `ValueError` so a sister selection cannot silently land on Phase-4 by mistake. The aggregator-side branch on `latent_valid_override` and the encoder-side branch on `sa_mask_mode` stay open under the joint-phase trainer build (#97 B2.2); this row tracked the dispatch-surface absence, which is now closed. |
| **✅ CLOSED 2026-05-29 [code-vs-spec]** [Verifiable: `grep -n 'loss_variant: LossVariant = "b31_default"' src/speech_decoding/experiments/v14_joint.py` → line 190; `ssl/aggregator.py` `loss_variant` selector (`b31_default` = 2 terms) + `variant_wants_m3` / `variant_wants_utt`; `ssl/total_loss.py` `l_mid_slot` / `l_post_utterance` typed `Tensor \| None`, summed only when not-None; `experiments/v14_joint_module.py:139-157` `_V14StudentBundle` constructs `ln_frame` always but `ln_mid` / `ln_utt` / `pma` ONLY when `variant_wants_m3` / `variant_wants_utt` (default `b31_default` → all three `None`), forward gated identically at 424-439; dispatch CLI `--loss-variant` + `_validate_choice`. **70-test B31 matrix green** (`test_total_loss.py` + `test_aggregator.py` + `test_v14_joint.py` + `test_v14_joint_module.py` + `test_dispatch_v14_phase_flag.py`, 2026-05-29). **The P0 falsifier-sister RUNS** (`R-add-m3-loss` / `R-add-utterance-loss` / `R-add-both`) **remain a separate experimental gate — the code arms exist, the DCC runs do not.**] — **B31 V-JEPA-2-canonical 2-term SSL + PMA-trained-only-at-P3 lock** (2026-05-28 PM, canonical lock [[project_v14_b31_vjepa2_canonical_loss_2026_05_28]]): Joint SSL phase collapses 4-term → **2-term** default: `L_total = L_pre_frame_masked @ M2 + L_post_frame @ M4` (V-JEPA 2 §2.1 Eq 1, pure L1, EMA τ=0.999 full-input teacher per B26/B27). DROP `L_mid_slot @ M3` + `L_post_utterance @ M4-PMA` from the default loss aggregator. **PMA is NOT trained in the joint SSL phase** — `V14ParcelCollapsePMA(freeze=True)` default at `models/v14_encoder.py:1225` is now spec-correct (closes the long-standing B19-vs-code drift). PMA gets gradient at P3 only (Whisper-L8 distillation against `PMA(M4)`), then frozen at P4 (`v14_encoder.py:1526` already passes `freeze=True`). **Rationale**: no precedent in the Perceiver-family for loss at post-cross-attn / pre-process state (M3); V-JEPA 2.1 §2.3.2 DSS is motivation-only per B28; iBOT CE-on-soft-tokenizer ≠ v14 L1-on-features. Utterance signal arrives at P3 via Whisper-L8 distillation. Geeling minimalism. Falsifier sisters (P0 must-run before pre-Stage-1 lock): `R-add-m3-loss` (puts `L_mid_slot` back at coeff 1.0), `R-add-utterance-loss` (puts `L_post_utterance` back at coeff 1.0; restores PMA-trained-at-P1+P2 contract), `R-add-both` (both terms back; restores B19 4-term). EAT (Chen 2024 IJCAI arXiv:2401.03497, +1.3% mAP at λ=1.0 on AS-2M Fig. 4) cited as the singular utterance-loss precedent on the `R-add-utterance-loss` sister; promotion gate = ≥0.02 AUROC at BT-Lite Cell-0 scale. | `ssl/aggregator.py:compute_v14_ssl_losses` is the **4-term aggregator** wiring `l_pre_frame + l_mid_slot + l_post_frame + l_post_utterance` and threading `pma_student`, `pma_teacher`, `student_m3_lnmid`, `teacher_m3_lnmid`, `student_m4_lnutt`, `teacher_m4_lnutt` through `v14_total_loss`. `ssl/utterance_loss.py` (`utterance_mse_loss` + `pma_then_mean`) and `ssl/slot_loss.py` (`masked_mse_slot_time`) are intact loss primitives — they survive as sister-cell-only modules (no caller in default after B31). `models/v14_encoder.py:1208-1272` `V14ParcelCollapsePMA(freeze: bool = True)` default IS spec-correct under B31 (the joint-SSL Experiment must NOT pass `freeze=False` to PMA construction). `experiments/v14_joint_module.py` (`V14JointBrainModule`, per [[project_v14_b29_joint_default_2026_05_27]] Item 1 closure) still wires the 4-term aggregator. | (1) `ssl/aggregator.py:compute_v14_ssl_losses` — drop `student_m3_lnmid` / `teacher_m3_lnmid` / `student_m4_lnutt` / `teacher_m4_lnutt` / `pma_student` / `pma_teacher` kwargs in default path; drop the `l_mid_slot` and `l_post_utterance` calls; tighten the return to `V14TotalLossBreakdown` only when default (sister-mode keeps the tuple). (2) `ssl/total_loss.py:v14_total_loss` — make `l_mid_slot` and `l_post_utterance` optional (default None) and skip in `.total` sum when None. (3) `experiments/v14_joint_module.py` `V14JointBrainModule._compute_v14_ssl_losses` callsite — stop forwarding the 4 dropped tensors in default mode; stop constructing `pma_student` / `pma_teacher` on the SSL graph in default; do NOT instantiate `LN_mid` / `LN_utt` / `loss_mid_head` for the default SSL Experiment. (4) `models/v14_encoder.py:1208-1272` — KEEP the class as-is (P3 + sisters need it). Just stop constructing it in the joint-SSL forward path under default. The `freeze=True` default is correct. (5) Add dispatch flag: `loss_variant: tp.Literal["b31_default", "b31_plus_m3", "b31_plus_utt", "b31_plus_both"] = "b31_default"` on `V14JointExperiment`. Sister-arm logic in `V14JointBrainModule` constructs the dropped heads / forwards the dropped tensors conditionally. (6) Tests: update `ssl/test_aggregator.py` to assert default 2-term arithmetic (`L_total == L_pre_frame + L_post_frame`) AND assert sister flags yield 3-term / 3-term / 4-term variants. Move `ssl/test_utterance_loss.py` BUG-1 / CHECK-3 / BUG-3 tests behind a sister-mode pytest mark (they test the `R-add-utterance-loss` arm). Update `experiments/test_v14_joint_module.py` 4-term breakdown assertions to default-2-term + sister-conditional 4-term. (7) Recipe doc already amended (`docs/neuroprobe/training_recipe.md §3` B31 banner + bullet annotations); MEMORY.md canonical table already shows B31 wins. |
| **✅ MODULES LANDED 2026-05-30 [code-vs-spec; P3 dispatch wiring + P4 probe OPEN]** [Verifiable: `grep -n 'class StudentWhisperProjector' src/speech_decoding/models/whisper_adapter.py`; `grep -nE 'def fit_channel_stats\|class TargetStandardizer' src/speech_decoding/bt_alignment/teacher_cache.py`; `WHISPER_CONTRACT["project_direction"]=="up"` + `["target_standardization"]=="per_channel_zscore_train_only"` + `["teacher_down_adapter"] is False` + `["student_head"]=="mlp_256_1280"` in `bt_alignment/contract.py`; param pins `StudentWhisperProjector` mlp `== 1_968_640` / linear `== 328_960` in `models/test_whisper_adapter.py`; standardizer tests (train-only, zero-variance guard → `inv_std==1`, unit-var/zero-mean, fp32 dtype) in `bt_alignment/test_teacher_cache.py`; project-up `(B,40,1280)` shape test in `ssl/test_distill.py`; project-up contract test in `bt_alignment/test_contract.py`. 46-test touched-file matrix green 2026-05-30.] — **B33-project-up** (2026-05-30, canonical lock [[project_v14_b33_project_up_phase3_2026_05_30]] + handoff `docs/neuroprobe/b33_project_up_code_handoff.md`): P3 distillation flips project-DOWN → **project-UP**. Teacher target = fixed full **1280-d** Whisper all-layer mean (no teacher-side trainable module); student projects **256→1280** via new `StudentWhisperProjector` (default `mode="mlp"`, LLaVA-1.5 ~1.97M; `R-head-linear` sister = single `Linear` ~0.33M); loss in 1280-d. Per-channel **train-only z-score MANDATORY** (`fit_channel_stats` two-pass fp32 + zero-variance guard → `TargetStandardizer` frozen affine), filling the StandardScaler role the demoted teacher adapter used to play. `WhisperAdapter` demoted → `R-project-down` sister. `ssl/distill.py` is shape-agnostic (no math change; docstrings clarify the two normalization axes: mandatory per-channel corpus z-score UPSTREAM vs opt-in per-token `target_instance_norm`). P4: **SETTLED by B35** (reverts the transient attentive probe) — frozen-PMA → mean-over-time → linear; see B35-p4-frozen-pma-mean-linear row below. | **Modules + tests LANDED** in `src/` (2026-05-30): `StudentWhisperProjector` (`models/whisper_adapter.py`), `fit_channel_stats` + `TargetStandardizer` (`bt_alignment/teacher_cache.py`), `WHISPER_CONTRACT` project-up fields + fixed standardization docstring (`bt_alignment/contract.py`), project-up docstrings (`ssl/distill.py`). `WhisperAdapter` (now `R-project-down`) + `WhisperLayerMerge` retained; `extractors/whisper_teacher_pool.py` unchanged (already 1280-d, direction-agnostic). | (1) **P3 dispatch / training-loop wiring** (OUT of this pass): 3a freeze-encoder train PMA+head ~3–5k; 3b unfreeze A@LR/10 + B@/3 + PMA+head@full ~18k; LR param-groups; EMA=none; early-stop on val Smooth-L1 — `dispatch_v14.py` + NeuralTrain Experiment, still unwired (P3 has no dispatch yet). (2) Fit `channel_stats.pt` over the P3 train-pool + load into the P3 dataloader; apply `TargetStandardizer` to the cached target before the loss. (3) Expose sisters as flags: `R-project-down` (teacher-side `WhisperAdapter` + 256-d loss), `R-head-linear` (`StudentWhisperProjector(mode="linear")` — module done, flag pending), `R-no-target-standardize` (bypass standardizer), `R-frozen-throughout` (freeze encoder all P3 — training-loop flag). (4) **P4 readout SETTLED by B35** — frozen-PMA → mean-over-time → linear (see B35-p4-frozen-pma-mean-linear row below); the B34 attentive probe is reverted. Decision gate: `R-head-linear` vs MLP default judged on P4 downstream transfer, never on P3 distillation loss; both must-run. |
| **B35-p4-frozen-pma-mean-linear** (2026-05-31, REVERTS B34; canonical lock [[project_v14_b35_p4_frozen_pma_mean_linear_2026_05_31]]): **Phase-4 readout = frozen-P3-PMA → mean-over-time → per-task Linear.** Frozen PMA collapses the parcel/slot axis (L=80 → 1) per time-patch → `(T_p, d)`; mean over T_p → `(d)`; `Linear(d, n_classes)`. **Trainable at P4 = the linear ONLY** (514 binary / 2,570 10-way); frozen PMA = 263,424; full P4 model ~12.12M < 30M. PMA RETURNS to P4 (frozen) — reverts B34's drop-PMA. **mean-over-time not flatten** (PopT-precedent: PopT has no time axis at readout; + empirical bucket-A: the whole leaderboard margin is in presence/magnitude tasks, B/C near-chance for the field). **Reason for revert:** B34's 792k-trainable attentive query isn't trainable at Neuroprobe's ≤3500-sample/task budget with the encoder frozen (~152–453× params-over-data). Deferred if-time sisters: `R-p4-flatten` (=B31 flat-head, 10,250) / `R-p4-time-attn-pool` (~257) / `R-p4-attentive` (=retired B34, 792k) / `R-p4-meanpool-no-pma`. | **B34's P4 code LANDED in `src/` (uncommitted) and now DIVERGES from B35**: `V14PerTaskAttentivePooler` (default) + `V14MeanPoolLinearHead`; PMA REMOVED from P4; config `readout: {attentive, meanpool}`; CLI `--readout`; `pma_freeze` retired. Also inherits a param-count error (B34 docs say PMA "789,760"; real = 263,424). `V14ParcelCollapsePMA` exists (P3) but is no longer constructed at P4. | (1) Re-add the frozen P3-PMA at P4 (construct + load from P3 ckpt + `requires_grad_(False)`). (2) New default readout `pma_mean_linear` = frozen-PMA → mean over T_p → `Linear` (NOTE the existing `V14MeanPoolLinearHead` means over parcel×time, NOT this — it becomes the `R-p4-meanpool-no-pma` sister, not the default). (3) `readout` selector `{"pma_mean_linear" (default), "pma_flatten_linear", "pma_timeattn_linear", "attentive", "meanpool"}`; PMA constructed + frozen for the first three. (4) `V14ParcelCollapsePMA` docstring "Phase-3 ONLY" → "P3-trained, P4-frozen". (5) Update tests + param assertions (frozen PMA 263,424; linear 514/2,570; total ~12.12M); correct the 789,760 → 263,424 error in any test pins. (6) Full handoff: `reports/v14_b35_p4_frozen_pma_mean_linear_handoff_2026_05_31.md`. |
| **✅ CLOSED 2026-05-28** [Verifiable: `grep -nE 'clip_valid' src/speech_decoding/ssl/utterance_loss.py` returns kwarg at line 52 + dilution-free divisor block ~86–107; `pma_then_mean` returns `(pooled, clip_valid)` tuple with mixed-batch SWEC routing; 6 unit tests in `ssl/test_aggregator.py` + 6 dedup tests in `ssl/test_utterance_loss.py`] — **B30-pma-zero-active-guard** (2026-05-28, canonical lock [[project_v14_anatomy_gated_symmetric_2026_05_28]]): **When `latent_valid.sum(dim=-1) == 0` for a clip (SWEC under B30 default), skip PMA + L_post_utterance entirely for that clip** to avoid softmax-over-empty-set NaN. Aggregator-level filter: clips with zero active slots contribute zero to L_post_utterance (denom-clamped). If the entire batch is SWEC, L_post_utterance term = 0; L_pre_frame_masked still fires. | `V14ParcelCollapsePMA` currently assumes ≥1 valid slot per clip; SWEC pass-through would softmax over zero unmasked positions → NaN. | (1) In the training Experiment loss aggregator, compute `clip_has_active_slots = latent_valid.any(dim=-1)` and filter PMA inputs to `m4[clip_has_active_slots]` + `latent_valid[clip_has_active_slots]`. (2) Aggregate L_post_utterance over the filtered subset; multiply by `clip_has_active_slots.float().mean()` to keep loss magnitude on the same per-batch scale as L_pre_frame_masked. (3) Runtime assert: `(latent_valid.sum(dim=-1) > 0).all() or current_batch_has_anatomy_blind_subjects` (guards against the bug-pattern where BT clip accidentally gets latent_valid all-False). (4) Unit test in `test_utterance_loss.py`: passing a batch where row 0 has all-False latent_valid and row 1 has some True returns finite loss (no NaN), and the loss equals the L1 over row 1 alone × `clip_has_active_slots.float().mean()`. |

**Knock-on**: B20 spec was locked 2026-05-24 (hop=256 + Conv2d patches + 10-vec freq embed), but the code still runs the v3 per-cell linear stem at hop=128. Any Phase-1 smoke run before these are wired is testing the wrong architecture. Treat as a soft-blocker on Phase-1 dispatch (the smoke tests will pass but the scientific claim won't be what's described in the paper). B27 (5/27 PM-late, supersedes B25+B26 context-loss parts; keeps B26's L1 + fixed-EMA + teacher-contract parts) — Phase-1 dispatched without B27 would (a) silently run MSE instead of pure L1 (V-JEPA-2-canonical loss form per V-JEPA 2 §2.1 Eq 1), (b) silently run the V-JEPA 1 EMA ramp pattern that V-JEPA 2 §2.4 explicitly retired as unnecessary, (c) have an ambiguous teacher-input contract that could degenerate the JEPA supervision signal. B27 does NOT require the spurious context-loss + warmup wiring B26 specified — the default is V-JEPA-2-canonical masked-only at M2, which is the correct regime for v14's clip-level-only downstream profile (V-JEPA 2.1 Tables 1+2 show context loss costs ~10pp on clip-level SSv2 with no Deep Self-Supervision rescue available at v14's heterogeneous-depth structure; see B27 memo).

**Settle-by**: each row resolves with the listed code change + a `Verifiable as:` grep target identical in form to other ✅ Decisions. Track as a BIG meta-blocker pending closure of all twenty rows.

---

**Walk-through protocol.** Pick lowest open ID. Read entry. Pick a default or push back / refine. Mark ✅ + Decision in this file + cascade to canonical memo if it changes the contract. **Every new ✅ Decision must end with a `**Verifiable as**:` line** listing the literal symbol(s) / value(s) the B18 closure-gate audit will grep for, with target file path (e.g., `betas=(0.9, 0.95), weight_decay=0.05` in `src/speech_decoding/experiments/v14_phase1.py`). Decompose compound decisions into one `Verifiable as:` row per sub-claim — single grep on a headline value silently passes while sub-clauses drift (B02 has ~10 sub-claims; B08 has 3; B01 has ~8). Prose-only decisions retrofit later cost much more than baking the grep target in at decision time. Existing 13 ✅ Decisions are backfilled as B18 step 1.

---

## BIG gaps (resolve before any submitter)

### B01. ✅ Phase 1/2/3 optimizer + LR + schedule (2026-05-23, v3 lock)
Canonical: `memory/project_v14_three_phase_staged_recipe_2026_05_18.md` top-of-file v3 amendment block.

- **Shared** (all phases): AdamW (β1=0.9, **β2=0.95**, wd=0.05, eps=1e-8), grad_clip=1.0, two param groups (no-WD for biases, LN/RMSNorm γβ, 320 free latents, freq embedding).
- **Precision**: bf16 mixed-precision; fp32 master weights + AdamW state + EMA buffers. Front-end (Multi-STFT → filterbank → log → robust-z) runs fp32 (autocast disabled). log-ε = 1e-6.
- **Distributed**: DDP only (no FSDP/ZeRO at 15M post N=4→6 amendment). `gradient_as_bucket_view=True`, `static_graph=True`. Activation checkpointing on token blocks + latent stack is a default-**OFF** knob (`--gradient-checkpointing`; code default `gradient_checkpointing=False`) — off on 80 GB nodes (DeltaAI H100), on for memory-bound configs (e.g. bs=32 on 32 GB coganlab-gpu). Bit-identical numerics; gated on `training and grad_enabled` so the no_grad EMA-teacher pass is never checkpointed.
- **LR-batch scaling**: √-rule, `LR_eff(B) = LR_ref × √(B/B_ref)`, valid to ~64k effective batch (Cherti 2024).

| Phase | Trainable | Peak LR @ B_ref | Warmup | Schedule | Steps | Eff. batch |
|---|---|---|---|---|---|---|
| 1 | A1 + freq + 6 token blocks + 2-block predictor (~6.6M; N=6) | 5e-4 @ 1024 | 20k linear | cosine→0 | 400k (sisters 300k) | 1024 |
| 2 | Stage B latent + cross-attn + PMA (~8.8M) | 3e-4 @ 512 | 5k linear | cosine→0 | 40k | 512 |
| 3a (warmup) | adapter (~0.8M) | 3e-4 @ 256 | 500 linear | constant | 2k | 256 |
| 3b (unfreeze) | adapter @ 4.2e-4; B @ 1.4e-4 (/3); A @ 4.2e-5 (/10) | derived | 1k Stage-A-only ramp | cosine→0 | 18k cap + early-stop | 512 |

- **Loss family**: MSE on instance-normed teacher latents (per-token across feature dim, BEFORE K-averaging) for P1+P2; **K=6 in both P1 and P2** (all N/L layers, symmetry rule). Smooth-L1 raw on un-normalized Whisper-L8 for P3 (deletes prior "instance-norm on Whisper side" line as misapplied data2vec-2.0 to frozen-external-teacher setting).
- **λ_frame:λ_utt = 1.0:1.0** in P2 (EAT precedent). [Closes B04 λ-ratio.]
- **EMA momentum** (span FULL phase, not warmup): P1 linear 0.99 → 0.9999 over 400k; P2 linear 0.999 → 0.9999 over 40k. [Closes B11 target-K + clarifies span.]
- **Stage-A predictor (P1)**: 2-block transformer, hidden=128, heads=4, ~0.2M. Warm-started to P2 (B03c PM revision), discarded at P2→P3. (P2 has no separate predictor beyond P1's; PMA + cross-attn play that role for utterance head.)
- **LayerNorm on latent Q-stream** entering each cross-attn (Perceiver-IO canonical; collapse insurance).
- **Valid-bin fill ordering**: log → robust-z (skip invalid per mask) → fill=0 (post-z) + parity test. [Partial — IM11 separately closed.]
- **P3b early stop**: val Smooth-L1, 5% held-out-by-session, patience 5 ep, eval ~1k steps.
- **B_crit monitoring hook**: log per-step grad norm + per-corpus contributions during P1 first 20k steps.
- **Sister cells (P1)**: `S-constant-p1` (DINOv3 constant LR), `S-wsd-p1` (OmniMouse WSD with cooldown @ 200k), `R-K-last-only`, `R-no-target-norm`, `R-predictor-mlp`.
- **Contingency**: L2-SP regularizer on Stage A vs P2 ckpt (λ=1e-4) if P3b shows Stage-A drift.
- **Confidence at lock**: ~88% global / >95% on high-impact items; remaining ~12% in {effective-batch, P1/P2 step counts, EMA endpoint, predictor shape} all sister-cell-falsifiable or M0-empirical.
- **Verifiable as**: `betas=(0.9, 0.95), weight_decay=0.05` in `experiments/v14_phase{1,2,3}.py`; param-group split via `no_wd_param_names` predicate; `torch.cuda.amp.autocast(enabled=False)` wrapping front-end ops; DDP `gradient_as_bucket_view=True, static_graph=True`; per-phase step counts + warmup match the table above.

### B02. ✅ Cross-corpus batch composition Phase 1 (2026-05-23, 3rd re-lock)
- **Decision**: **α=0.5 hierarchical over EXACT-PRECOMPUTED valid-bin-electrode-hours (vb-eh)**. Two-group: SWEC vs broadband (AJILE12+D+BT). Macro **50/50** (hard SWEC cap); within broadband **α=0.5** over vb-eh. Per-row weight `= group_share × within_group_share / count_rows[source]`. Estimated shares: SWEC 50.0 / AJILE12 ~27.7 / D 15.0 / BT 7.3 (refined at sampler-build).
- **Sampler stack**: `WeightedRandomSampler(replacement=True)` over per-row vb-eh weights, wrapped by `torchdata.stateful_dataloader.StatefulDataLoader` (native `state_dict`/`load_state_dict` for mid-epoch resume; per-rank deterministic worker RNG; `persistent_workers=True`). Per-rank seed `Generator().manual_seed(base_seed + rank + epoch)`.
- **vb-eh base unit**: `vb_eh[corpus] = Σ_session (hours × n_electrodes × |valid_bins[corpus]|)` computed from manifest at sampler-build (exact totals, not corpus-medians). AJILE12 valid k0–k20 (500 Hz / 0.5–200 Hz, Peterson 2022 — independent correctness fix vs prior "all 30 bins" mislabel); SWEC k0–k21 (Charmander reference 0.5–120 Hz); D+BT k0–k29.
- **Loss reduction**: row-mean-then-batch-mean (MAE/V-JEPA), fp32 accumulator: `(loss * mask).sum() / mask.sum().clamp(min=1)` regardless of bf16 forward.
- **SWEC locality sharding**: at job-start, partition SWEC subjects across ranks deterministically (`{s : hash(s) % W == r}`); rank `r`'s WRS draws only from its local SWEC subset (assignment static across epochs → kernel page-cache amortizes; MosaicML StreamingDataset / Megatron `BlendedDataset` pattern). Broadband corpora mmap globally.
- **shaftCAR-on-grid documented**: AJILE12 ECoG grids collapse into one CAR group (`CARIeegExtractor` parses trailing-int suffix) → effectively global-CAR for AJILE12, different reference operator than within-shaft sEEG. Nv14 per-(electrode, freq) z absorbs amplitude shift; bin-relative shape differences survive as a known per-corpus property.
- **Sister cells** (P1, `ablations.md §6j`): `R-sampler-alpha03`, `R-sampler-sqrth`, `R-sampler-pure-h`, `R-sampler-uniform`, `R-sampler-broadband-uniform` (SWEC 30 / AJILE 30 / D 25 / BT 15), `R-sampler-seeg-only`, `R-sampler-40-60`, `R-sampler-60-40`.
- **Coupling**: locks DP02 sampler formula P1; B01 effective batch = 1024 inside XLS-R's α=0.5 validated regime (sister sweep mandatory if batch ever ≥ 4096 per Data Mixing Laws, Ye 2024). Cross-refs: BP22 (domain-match vs scale), BP20 (subject overlap — orthogonal), IM11 (high-bin under-training accepted, `R-sampler-broadband-uniform` falsifier).
- **Precedent**: XLS-R §4.1 (Babu 2022 arXiv:2111.09296 verbatim "α=0.5 in all cases") + MMS §4.2 (Pratap 2023). DINOv3 §3 uses identical 10/90 macro split. UniMax (ICLR 2023) on hard caps for heterogeneous quality. Full citation map in `memory/project_v14_cross_subject_pretraining_data_strategy_2026_05_22.md`.
- **Verifiable as**: algorithmic primitives in `src/speech_decoding/experiments/wrs_sampler.py` — `ManifestRow` (corpus, n_electrodes, hours, valid_bins) with `vb_eh() = hours × n_electrodes × valid_bins`; `compute_per_row_weights(rows, alpha=0.5, macro_split={"swec":0.5,"broadband":0.5})` returns per-row `np.ndarray` summing to 1.0 with `w[row] = group_share × within_group_share / count_rows[corpus]`; `build_wrs_sampler(weights, num_samples)` constructs `WeightedRandomSampler(..., replacement=True)` (per-rank `Generator` injected via `generator=`); `build_stateful_dataloader(...)` lazy-imports `torchdata.stateful_dataloader.StatefulDataLoader`. Sister roster (α / macro) reached via parameter override. **Coverage**: 20/20 unit tests in `test_wrs_sampler.py` (vb-eh formula, macro 50/50, within-broadband α=0.5 share + α-sister overrides, BT-only / SWEC-only graceful collapse, validation rejects, replacement=True, generator wiring, 20k-draw empirical convergence, torchdata lazy-import).
- **Partial impl note (2026-05-28)**: per-corpus valid-bin mask `{swec: [0..21], ajile12: [0..20], d: [0..29], bt: [0..29]}` keyed at extractor init AND locality-sharding `subj_to_rank[s] = hash(s) % world_size` static-across-epochs are deferred to the multi-corpus loader build (separate B-bundle — `multi_corpus_loader.py` does not exist yet); `torchdata` is not yet listed in `pyproject.toml` (lazy-import raises `RuntimeError` with install hint until then). The vb-eh weight math itself is fully wired and tested; the only remaining work is plumbing it into the live multi-corpus DataLoader once that loader exists.

### B03 bundle (B03 / B03b / B03c / B03d / B03f) — ✅ Mask-discipline lock (2026-05-25 PM)

**Shared bundle context**: five linked questions about mask substitution + supervision-set semantics in P1/P2 (electrode mask paradigm, latent-SA slot gate, patch-mask substitution paradigm, EMA teacher asymmetry, JEPA-correct supervision set). All closed jointly under `memory/project_v14_b03_mask_lock_2026_05_25.md` (canonical). Headline: v14 is V-JEPA-family — drop semantics for both mask layers, asymmetric teacher in P2, per-subject anatomy gate.

#### B03. Phase 2 electrode-mask mechanism
- **Question**: replace shaft-masked electrode's `(F,T,d)` token slab with (a) zeros, (b) learnable `[MASK]`, or (c) drop entirely.
- ✅ **(c) DROP** via `key_padding_mask = pad_mask_C_MAX | shaft_mask` consumed by cross-attn ❺a/❺b (shaft_mask = False outside P2 student). ~40% cross-attn cost reduction on P2 student forward. Symmetric with B03c patch-mask drop; consistent with v14's JEPA-family lineage (rejects biosignal-FM paradigm A precedent of LaBraM/Brant-2/REVE).
- **Verifiable as**: `cross_attn.key_padding_mask = pad_mask | shaft_mask` in `models/v14_encoder.py` (❺a + ❺b); `shaft_mask` default `torch.zeros_like(pad_mask)` outside P2 student; integration test asserting P2-student cross-attn cost drops ~40% vs P1.

#### B03b. Latent SA parcel-supervision gate
- **Question**: 320 latent slots pre-allocated; per-subject only `parcels_supervised[subject]` slots carry signal. Should others participate in latent SA?
- ✅ Latent SA ❻ `key_padding_mask = ~slot_mask(parcels_supervised[subject])` in ALL phases (P1/P2/P3/P4), carried across phase boundaries. Documents contract for code already implementing it.
- **Verifiable as**: `latent_self_attn.key_padding_mask = ~supervised_slot_mask` in `models/v14_encoder.py`; unit test `supervised_slot_mask.sum() == M × len(parcels_supervised[subject])` per BT subject.

#### B03c. Per-electrode patch-mask substitution paradigm
- **Question**: paradigm A (`[MASK]` token, fixed F×T grid) vs paradigm B (drop + predictor, visible-only).
- ✅ **Paradigm B = drop + 2-block predictor** (hidden=128, heads=4, ~0.2M, V-JEPA/Brain-JEPA/EAT/data2vec-2.0/MAE family). **Predictor warm-starts from P1 ckpt at P2 step 0** (PM revision — no JEPA precedent for inter-phase predictor re-init; re-init would spike L_pre_frame under simultaneous bias-flip). Discarded ONCE at P2→P3 (Whisper distillation doesn't need it). Sister `R-p2-predictor-reinit` (P1) retains the re-init alternative for attribution. Sister `BiL-B03-PatchMask-Paradigm-A` (P0 must-run) tests the substitution paradigm.
- **Verifiable as**: `Predictor2Block(d=d, hidden=128, heads=4, depth=2)` in `models/v14_encoder.py`; `predictor.requires_grad_` True only in P1+P2; `predictor.load_state_dict(p1_checkpoint['predictor'])` at P2 step 0; P1 end checkpoint INCLUDES predictor state_dict keys.

#### B03d. EMA teacher asymmetry in P2
- **Question**: does teacher see student's patch mask + shaft mask in P2?
- ✅ **Teacher receives FULL electrodes AND FULL patches** (no shaft mask, no patch mask on teacher). Asymmetry IS the JEPA supervision signal. Valid-bin mask (EX09) stays symmetric — different layer (input-domain validity, not SSL masking). Sister `R-p2-teacher-shaft-mask-symmetric` STAYS P1 as **falsification cell**, NOT a mean-collapse mitigation (symmetric teacher would destroy the V-JEPA signal). Mean-collapse mitigation is instead new P0 sister `R-stratified-shaft-mask` (avoids uniform-random shaft selection making some parcels orphan in nearly every clip). Pre-registered `MON-MASK-002` = bounded MSE ratio `orphan_MSE / visible_MSE ∈ [0.7, 1.5]`; > 50 consecutive out-of-band steps triggers escalation. **Under K=1 default (2026-05-27 PM)**: `ratio < 0.7` (mean-collapse) → escalate to `R-stratified-shaft-mask`; `ratio > 1.5` (student failing orphan task) → escalate UP to `R-shaft-K2` (P0; sparser orphans under K=1 may be under-providing gradient), with `R-stratified-shaft-mask` as secondary investigation if shaft-selection uniformity is the root cause. (Prior K ≤ 2 era named `R-shaft-K1` as the step-down; that variant IS the default now, so the escalation reverses direction. Prior K=3 era named K=2 as step-down.)
- **Verifiable as**: in `experiments/v14_phase2.py`, teacher forward gets `electrode_mask=torch.zeros_like(...)` + `patch_mask=torch.zeros_like(...)`; unit test `teacher_forward_no_shaftmask.shape[-2] == C` every step; MON-MASK-002 assertion in P2 train loop.

#### B03f. Supervision-set semantics — `valid_parcels[clip]` is JEPA-inverted
- **Question**: B19's "`valid_parcels[clip]` recomputed from UNMASKED electrodes" REMOVES shaft-masked parcels from supervision — JEPA-inverted (the prediction target is exactly what should be supervised).
- ✅ **`parcels_supervised[subject]` replaces `valid_parcels[clip]` for SUPERVISION GATES only**; slot-bank REGULARIZERS revert to all 320 slots (B21 default). Scope split:
  - **(a) Loss-side supervision gates** → `parcels_supervised[subject]` (per-subject DK parcels with ≥1 electrode in anatomy; computed ONCE at extractor init, invariant to within-clip masks). Gates `L_mid_slot` + `L_post_frame` + `L_post_utterance` PMA softmax. Shaft-orphaned parcels REMAIN supervised — that IS the JEPA task.
  - **(b) Slot-bank regularizers → all 320 slots** (PM revision): `L_DKoleo`@M4 (B21) + `L_DKoleo`@M3 (B22) + reactive Gram + monitor F1 operate over the full bank. Reason: B21 collapse-prevention math relies on cross-subject `LearnableParcelEmbed[p]` reachability across the entire bank; scoping to `supervised_idx` per-batch leaves unsupervised parcels' embeddings untrained.
  - **(c) Subject-ID leakage canary**: `parcels_supervised[subject]` is a deterministic per-subject set → encodes subject identity as a side channel. Pre-registered `MON-MASK-004` (P2 step ~30k subject-ID linear-probe F1 > 0.50 vs 11% chance baseline) flags leakage that would corrupt CSubject gate. Sister `R-per-clip-pre-mask` (P0 must-run) tests per-clip pre-mask supervision sets as a falsifier.
  - SWEC fallback: `parcels_supervised[subject] = ∅` (no-op on entire-batch supervision when anatomy missing). Sister `R-clip-level-valid-parcels` (P1) retained as JEPA-inversion reversion test on (a).
- **Verifiable as**: `parcels_supervised: dict[subject_id, set[parcel_id]]` precomputed in `extractors/parcel.py`; `assert parcels_supervised[subject] == parcels_covered[subject]`; grep `valid_parcels\[clip\]` returns 0 hits in active loss sites of `experiments/v14_phase{1,2}.py` (only in sister `R-clip-level-valid-parcels`); grep `supervised_idx` indexing on DKoleo/monitor-F1/reactive-Gram returns 0 hits (all 320); MON-MASK-004 probe at step-30k ckpt.

### B04. ✅ Phase 2 loss weighting + utterance-teacher (2026-05-25 PM, ratified from B01 + B03d + B22)
- ✅ **λ_utt = 1.0** (B01 5/23 EAT precedent; supersedes default-candidate 0.5). **Single shared EMA teacher, asymmetric** — teacher sees full electrodes + full patches in P2 (B03d), applies its own dedicated `LN_mid_T / LN_frame_T / LN_utt_T` heads at the three M-tap divergence points (B21/B22 head-split), computes PMA-k=1 over its full-electrode M4. Student PMA over masked-input M4 → supervised toward teacher PMA over full-input M4 (asymmetry IS the supervision signal). NO separate `[utt-teacher]` EMA buffer — same EMA params, single forward pass.
- **Verifiable as**: one `teacher_forward(x_unmasked)` call in `experiments/v14_phase2.py` returns `(M2_T, M3_T_LN_mid, M4_T_LN_frame, M4_T_LN_utt_PMA)`; loss sum has `1.0 * L_post_utterance` coefficient and no other utterance coefficient anywhere; grep `EMATeacher` returns exactly 1 instantiation in P2 train loop.

### B05. ✅ Triangular-pool exact spec — teacher-side (2026-05-25 PM, joint with B06)
- ✅ **Teacher-side pool only.** Student stays at 8 Hz native (B20 v4); teacher pools Whisper 50 Hz → 8 Hz (factor 6.25).
  - **Location**: applied to frozen Whisper-L8 hidden state in dataloader: `(250, 1280)` per 5s clip → `(40, 1280)`. Parameterless, cached.
  - **Kernel**: triangular, FWHM = 250 ms (2× output bucket spacing); each bucket pulls ~12.5 Whisper frames with linearly-decaying weights.
  - **Normalization**: sum-to-1 per output bucket. **Edge handling**: zero-pad at clip start/end.
  - **No student-side pool**: v14 outputs `(40, 256)` natively at 8 Hz × 5s; identity passthrough into Smooth-L1.
- **Cascade**: 5/22 pivot §3 + recipe §"Stage 3b" update `(50, 256)` → `(40, 256)`.
- **Verifiable as**: `TeacherTriangularPool(in_hz=50, out_hz=8, fwhm_ms=250)` in `experiments/v14_phase3.py` or extractor; output `(B, 40, 1280)`; `assert weight_matrix.sum(dim=-1).allclose(torch.ones(40))`.

### B06. ✅ Phase-3 teacher rate + layer — 8 Hz / L8 (2026-05-25 PM, joint with B05)
- ✅ **Teacher rate = 8 Hz, teacher layer = Whisper-L8.** Demotes ridge sweep from gate to sister-cell falsification. Three convergent timescales bracket teacher rate to 4–8 Hz: (a) English syllable rate 5.3 ± 1.5 sps (Pellegrino 2011); (b) cortical speech-tracking theta band 4–8 Hz (Luo & Poeppel 2007, Ding & Simon 2014); (c) Whisper-L8 = acoustic-phonetic content (Antonello/Shimizu), phoneme rate ~12 Hz too fast for L8's effective representation, syllable rate is the right unit. 8 Hz is the high end of this band AND matches v14 student native rate AND avoids upsampling. Goldstein's 10 Hz sits just outside the principled range (round-up-from-syllable, not load-bearing).
- **Sisters (P1 falsification)**: `R-rate-5Hz`, `R-rate-10Hz` (Goldstein), `R-rate-16Hz` (2× student) — each ~1 GPU-h, ridge `Whisper-L8 @ r → HG envelope @ r`. If any beats 8 Hz by ≥5% Smooth-L1 on P3 val, re-lock to that rate.
- **Layer sisters (P2 optional)**: `R-layer-6`, `R-layer-10` — only if 8 Hz underperforms iEEG-SOTA in P3 val.
- **Cascade**: 5/22 pivot §5 retired (was ridge-sweep-as-gate); MEMORY.md teacher rate "10 Hz" → "8 Hz".
- **Verifiable as**: `teacher_rate_hz = 8` in `experiments/v14_phase3.py`; sister configs `R-rate-{5,10,16}Hz` in `ablations.md`; ridge-sweep preflight code absent.

### B07. ✅ PMA k=1 query training timeline (2026-05-23)
- ✅ **Option (b): trained in P1+P2 (via `L_post_utterance`) AND P3 (via Whisper-L8 Smooth-L1), frozen at P4 linear probe.** Non-degenerate in P2 because student input is shaft+patch-masked while EMA teacher sees full input — PMA learns robust clip-level pooling under electrode dropout (the cross-subject probing inductive bias). Front-loads ~220h paired P3 data with ~224h P2 unimodal SSL → ~2× effective gradient steps for the pool. ML precedent: learnable-pool-trained-during-SSL dominant ([CLS] in BERT/MAE/DINOv2/v3/BEiT, EAT utterance token, Perceiver-IO output queries; DETR object queries end-to-end). Sister `R-PMA-frozen-random` pre-baked for attribution.
- **Verifiable as**: `model.pma.query.requires_grad_(False)` called only at P4 linear-probe instantiation in `experiments/v14_phase4.py`; grep returns 0 prior `requires_grad_(False)` calls on PMA query in P1/P2/P3 code paths.

### B08. ✅ Phase 1 mask grid + rate + substitution (2026-05-23, amended 5/24 B19+B20, 5/25 B03c)
- ✅ 2D inverse block masking on the **F-patch × T-patch grid (10 × T_p)** post-Conv2d (3,2) per B20 v4 (NOT raw 5×6 (t,f) — superseded). Mask rate **≤ 50% per electrode** (HARD cap; was 60–65% pre-B19, narrowed to align with biosignal-FM modal + leave headroom for P2's shaft-mask stacking). Mask UNIT = patch. Substitution = **paradigm B = drop + 2-block predictor warm-started P1→P2** (B03c; ~0.2M, hidden=128, heads=4, discarded ONCE at P2→P3).
- **Sister `R-p1-frame-1d-keep-3-mask-80`** (P0, load-bearing) retains the original B08 audio-MAE-inheritance baseline (frame-1D, 80%, keep-3 ≈200ms on raw (t,f)) for empirical attribution. `BiL-B03-PatchMask-Paradigm-A` (P0) tests the `[MASK]` token alternative.
- **Citations**: V-JEPA 2.1, VideoMAE, EAT, Brain-JEPA, Laya/LeJEPA, REVE, FFTP/SpecMask 2025 — preserved in canonical memos.
- **Verifiable as**: `PatchMask(F_p=10, T_p=T_p, max_rate=0.5)` in `models/v14_encoder.py` P1+P2 paths; `Predictor2Block(d=d, hidden=128, heads=4, depth=2)` adjacent; runtime `assert mask_rate <= 0.5`; sister `R-p1-frame-1d-keep-3-mask-80` run config exists.

### B09. ✅ Latent-stack parcel SA bias — none (2026-05-25 PM)
- ✅ **Pure content attention over 320 slots.** Anatomy enters the arch exactly **once**, at cross-attn ❺ via `log(support[e,p] + ε)` + `LearnableParcelEmbed[p]` in slot identity (B21 init). Adding a second anatomy prior on latent SA (i) duplicates slot-identity information, (ii) bakes in geometric-distance ≡ functional-similarity (no iEEG evidence), (iii) at 9-subject P2 scale most `(p_i, p_j)` pairs reached by ≤ 1 subject → learnable bias barely trains. Cross-subject precedent: PopT, BarISTA, DIVER-1 all use pure content SA at parcel-level stage. Carried across all phases; latent SA receives only B03b parcel-supervision key_padding_mask.
- **Three P0 falsifiers** (`ablations.md §6g`): `BiL-LatentSA-Bias-BNA-Dist` (frozen Gaussian over DK pairwise cortical distance), `BiL-LatentSA-Bias-T5-Learnable` (learnable `B[p_i, p_j]`, ~6.7k params), `BiL-LatentSA-Bias-Graphormer-1Step` (DK adjacency 1-step). Promotion rule: ≥0.02 AUROC on P4 CSession or ≥0.02 on SSL Smooth-L1 val proxy → re-lock to that sister.
- **Cascade**: post-v3 amendment §0 ❻ updated with explicit no-bias note (silence was the gap B09 surfaced).
- **Verifiable as**: `LatentSelfAttn` in `models/v14_encoder.py` has NO `parcel_pair_bias` / `B_pair` / `parcel_distance_bias` Parameter or buffer; grep `parcel.*bias\|p_i.*p_j\|graphormer` returns 0 hits in latent SA module; sister configs `BiL-LatentSA-Bias-{BNA-Dist,T5-Learnable,Graphormer-1Step}` in `ablations.md`.

### B11. ✅ EMA teacher layer-K averaging (2026-05-23, amended same-day PM)
- ✅ **P1 K = all 6 token-block layers; P2 K = all 6 latent-stack layers**, both with **per-layer instance-norm before averaging** (data2vec 2.0 recipe). Symmetric all-layers usage; prior "P2 top-K=4 drop bottom-2 for deeper-semantic" pick dropped on Ben review (the bottom-2 latent-stack layers sit right after cross-attn @ position 0 carrying the freshest parcel-routed signal — exactly what the student should learn to predict).
- **Framework**: v14 IS JEPA (predictor + EMA teacher + student/teacher encoders); layer-averaging is a target-objective enrichment on top of JEPA. EAT/SSLAM/AaSP precedent JEPA+layer-averaged-target as explicit hybrid. Pure-final-layer JEPAs (I-JEPA / Brain-JEPA / V-JEPA-original / Laya / DINOv3) sit in regimes that don't transfer (image patch redundancy / fMRI smoothness / no-predictor self-distill / Hubel-Wiesel feature alignment).
- **Sisters**: `R-K-last-only` (K=1), `R-no-target-norm` (no per-layer instance-norm). Micro-ablation P2 K ∈ {1,3,4,5,6} sweep (~1 GPU-h, single BT-Lite subject) brackets below K=6 default.
- **Citations**: data2vec 2.0 §3.3 Fig 2 (K=1 strictly worse all modalities), EAT §3.2 (all layers), SSLAM 2025 ICLR (AS-2M 0.502 SOTA preserves EAT averaging), V-JEPA 2.1 §3.3 (multi-level concat).
- **Verifiable as**: teacher target computation in `experiments/v14_phase{1,2}.py` does `instance_norm(layer_k).mean(over layers)` across all 6 (token blocks P1 / latent stack P2); grep `top_k|last_layer_only` returns 0 hits in active teacher target computation.

### B12. ✅ bf16 attention-mask sentinel (2026-05-25 PM, B03 bundle dependency)
- ✅ **Hardcoded `MASK_NEG = -1e4` across all masked-softmax sites.** bf16's 7-bit mantissa truncates large negatives → `softmax(-1e4) ≈ 4.5e-5` per masked position (machine-zero for bf16/fp16); larger sentinels would underflow non-monotonically. Sites: cross-attn ❺a/❺b key_padding_mask (B03 drop), latent SA ❻ key_padding_mask (B03b supervised-slot gate), PMA softmax (B03f `parcels_supervised`), freq-SA invalid-bin (EX09). Precedent: HuggingFace `transformers` (`-1e4`/`-1e9` standard for this reason).
- **Unit test `test_bf16_mask_floor`**: all-invalid bias → `softmax(scores) < 1e-6` across bf16/fp16/fp32 for each of the 4 sites.
- **Verifiable as**: `MASK_NEG = -1e4` constant in `models/v14_encoder.py`; all `masked_fill_`/`masked_fill` call sites use `MASK_NEG` (≥4 grep hits); `test_bf16_mask_floor` covers each site.

### B13. ✅ Robust-z normalization scope (2026-05-25 PM)
- ✅ **Per-`(subject, session, electrode, freq-bin)` stats computed from each session's own recording, identically train and eval.** No cross-session pooling, no train-cohort blending, no test-side avoidance. Original framing overstated leakage risk: median/MAD is physical unit calibration (per-electrode impedance × amp-gain dominates over speech-label content at recording-long timescale); same category as amp-gain division or DC-offset removal. iEEG-FM field standard (BrainBERT §3.2, PopT §3.1, Brant §2.1, BarISTA, DIVER-1 all per-session including eval). Pooled-cohort alternative actively hurts: cross-electrode impedance differences manufacture systematic z-shift the model never saw. CSubject case forces this anyway (held-out subject has no train-side electrode IDs).
- **Methods paragraph (defensive)**: "Robust-z scaling is computed per (subject, session, electrode, freq-bin) from each session's own recording. The statistics depend only on the voltage marginal distribution over a recording-long window and contain no information about the speech labels. Same procedure applied identically at train and eval, in line with iEEG-FM precedent (BrainBERT, PopT, Brant). Pooled-cohort stats would create systematic per-electrode z-shift."
- **Sister `R-norm-cohort-pooled`** (P1, `ablations.md §6n`): per-(freq-bin) median/MAD pooled over training cohort applied to held-out CSubject test set; if wins/ties, the field-standard story shifts.
- **Cascade**: training_recipe.md §2 already implements the rule; B13's role is to make it explicit + add the falsifier.
- **Verifiable as**: `robust_z_stats[subject_id, session_id, electrode_id, freq_bin]` from session-own recording in `extractors/view.py`; grep `train_cohort_pooled_stats|train_side_robust_z` returns 0 hits in `extractors/`; unit test `fit(session_data).transform(session_data) == fit_transform(session_data)`; sister `R-norm-cohort-pooled` in `ablations.md`.

### B14. ✅ C_MAX electrode-count cap (2026-05-23 PM, CQ12 closure)
- ✅ **C_MAX=384.** Original "max 168" was wrong by 2×; live audit of `data/dcohort_manifest.csv` (n=128) gives D-cohort min 41 / mean 179 / p50 200 / p90 256 / p99 315 / max 366. AJILE12 ≈86–146 surface + ~5–50 depth ≤ ~200 (Peterson 2022 Table 2); BT ≤ 256; SWEC max 128 unique-subj (p99 126). 384 covers all four with 18-electrode headroom past D-cohort max.
- **Cost**: C only enters cross-attn (`@{0,3}`); token-block SA per-electrode (batch dim), latent stack 320×T (no C axis). Activation memory ~16 → ~26 MB fp16 (~1.6×). FLOPs negligible vs latent stack. HB02 unchanged.
- **Runtime safety**: `ValueError("subject {id} has {n} electrodes which exceeds c_max={c_max}")` raised in `extractors/{dk_support.py:68-70, view.py:323-325}` via `ElectrodeValidMask`; `test_valid_mask_handles_c_max_below_electrode_count` covers the raise.
- **Verifiable as**: `DEFAULT_C_MAX = 384` in `dispatch_v14.py:67`; `test_v14_dispatch_wired.py:45-46` asserts updated (8/8 pass). Memo: `[[project_v14_cq12_cmax_lift_2026_05_23]]`.

### B15. ✅ BT-film overlap with Whisper-large-v3 pretrain (2026-05-25 PM)
- ✅ **Accept + Goldstein-precedent defensive methods paragraph, no probe.** Original framing overstated leakage: Whisper has no clip-ID input, learned phonetic structure not film-specific embeddings; "hello" in LotR vs anywhere else yields essentially the same L8 vector. Actual effect = mild representation-quality bias for in-distribution audio (sharper phoneme boundaries) — bounded by Whisper already training on hundreds of thousands of hours of commercial English. Marginal Δ probably < 1%. Goldstein 2025 §Methods sets precedent (same concern with Whisper-medium↔podcast audio, resolved with one acknowledgment paragraph, reviewers accepted).
- **Methods paragraph (Ben drafts per IRONCLAD)** — defensive content to convey: BT stimuli include commercial films likely in Whisper-large-v3's pretrain; Whisper is a frozen generic encoder (no clip-level retrieval); resulting representation-quality bias is bounded and enables no clip-specific memorization; matches Goldstein 2025 §Methods.
- **No probe, no teacher swap, no sister** — empirical question doesn't justify engineering cost given the bounded mechanism.
- **Verifiable as**: methods paragraph addressing Whisper-v3 ↔ BT-film overlap exists in `paper/neuroprobe-hillclimb/methods/`; Goldstein 2025 §Methods cited; no `R-teacher-whisper-v1` or `L.7.FilmVintage` sister (intentional).

### B16. DCC compute feasibility — HB cluster (cache + GPU + wall-time)
- **Open subitems**: HB03 (Phase-2 224h convergence risk on small effective budget), HB06 (grad-checkpointing strategy for 32GB ceiling). HB01 + HB02 ✅ below.
- ✅ **HB01 (2026-05-23 PM)**: cache → `/work/ht203/cache/multi_stft/`, NOT `/hpc/`. `/work/` is 950 TB cluster, 233 TB free; `/hpc/group/coganlab/` is 1 TB lab-shared with 400 GB free (placing 5–8 TB blocks lab members). 75-day purge accepted — regenerate is embarrassingly parallel CPU (~25 hr on 32 workers via `MapInfra`), << one P1 GPU-run. CLAUDE.md rule scope-amended: persistent metadata/checkpoints/Whisper-L8 → `/hpc/`; bulk precomputes with cheap regenerate → `/work/`. I/O stack: numpy memmap + fixed locality sharding + **node-local NVMe stage-in** (10× speedup vs NFS, biggest lever) + DataLoader tune. Sister `R-no-nvme-stagein`. Memo: `[[project_v14_hb01_multistft_cache_location_2026_05_23]]`.
- ✅ **HB02 (2026-05-25, post-v4 re-cost)**: P1 budget = **~600 H100-h median (350–1,400 range across MFU)**, NOT 4,000 H100-h v3-era estimate. v3 tensor shape (F=30, T=73 = 2,190 patches/electrode) was never re-costed after 5/24 v4 invisible-frontend lock collapsed tokens to 200 patches (10× drop). v4 per-step: ~7.2e14 FLOPs × 400k = ~2.9e20 total; H100 dense 750 TFLOPS @ 35% MFU → ~305 H100-h single-card + ~2× overhead = ~600 H100-h median. Comparator: v14 at ~0.22 A100-h/data-h sits 5–20× above peers (BrainWave 0.010, Brant 0.106, MVPFormer 0.290 — within 30% of MVPFormer). **Cut-to-200k workaround retired** (full 400k fits in ~5–8 days on 2× H200 scavenger free at median MFU). Sister sweep is now the binding cost driver (7 × ~600 = 4,200 H100-h). Thesis budget ≈ 5,000 H100-h (P1 600 + P2 50 + P3 30 + 7 sisters 4,200). YC AWS $10k ($3/hr spot = 3,300 H100-h) + DCC scavenger-h200 (~1,000 H100-h after preempt) ≈ 4,300 H100-h — razor margin; any one of {ACCESS Explore 5–20k, NAIRR Start-Up 2k, EV $10k+ 3,300} adds 2–4× margin. **MFU micro-profile (1k-step, 8× Ada 5000 Cogan-lab, ~1d free) collapses 350→1,400 range to ±30%** — highest-leverage derisk. Memo: `[[project_v14_hb02_compute_estimate_2026_05_23]]` §POST-V4 RE-COST.
- **HB03/HB06 default**: M0 (Lite-cell rerun post-BTWordEvents fix) measures real throughput + Slurm preempt rate before committing P1 plan. Then: stream from `/work` with mid-job warm-cache, or grad-checkpoint every other latent-stack block to halve activation memory (TorchTitan §4, MosaicML Composer).
- **Settle-by**: HB03/HB06 close pre-Phase-1 dispatch; HB01/HB02 ✅.

### B17. ✅ Cross-attn anatomy time-invariance — chronic-implant scoping (2026-05-26)
- ✅ **Scope to chronic implants in three places.** `log(support[e,p] + ε)` time-invariance holds for chronic cohorts (BT/SWEC/AJILE12/D — stable electrode-tissue interface), NOT intra-op uECoG (PS cohort, within-session drift). v14 is a chronic-iEEG-FM paper; scope must be explicit to pre-empt reviewer ambush ("your routing breaks for intra-op clinical use case"). Three places:
  1. **Methods §Architecture** (Ben drafts per IRONCLAD): "The routing bias `log(support[e,p] + ε)` is time-invariant within a clip, valid under the chronic-implant assumption (no within-session contact drift). Extension to intra-op cohorts requires a time-varying support estimator (future work)."
  2. **Architecture-figure caption**: same sentence.
  3. **`v14_encoder.py` docstring**: drop misleading "strict 1:1 per timestep" claim (IE01 fix), add 1-line scope note.
- **Precedent**: PopT / BrainBERT / BarISTA all chronic-only with stated scope. Stronger reframe to "chronic-iEEG FM" in title/abstract rejected as load-weakening for universal-credentialing-artifact framing.
- **Sister `R-time-varying-support`** (future-work, PS-Stage-3+): parcel-level state-space estimator over `support[e,p,t]`. NOT a current-paper cell.
- **Verifiable as**: paper methods paragraph + figure caption in `paper/neuroprobe-hillclimb/methods/`; `v14_encoder.py` docstring grep 0 hits on `strict 1:1 per timestep`; docstring contains "chronic implant" scope note.

### B18. ✅ Closure-gate audit — decided defaults vs. actual implementation (META) — CLOSED 2026-05-26 (ledger = `docs/neuroprobe/v14_implementation_fix_list.md`)

> **Closure protocol**: the 4-agent audit (canonical memos × v14_blockers.md × training_recipe.md × `src/speech_decoding/`) produced `docs/neuroprobe/v14_implementation_fix_list.md` as the closure ledger. B18 closes when every item in that fix list is closed (or explicitly accepted as deferred per §D). The §G verification checklist replaces the per-row pass/drift/pending-impl protocol below. Implementation in flight 2026-05-27.

---

(Original B18 protocol kept below for audit-trail provenance — superseded by fix list closure ledger.)

- **Where**: every code file, memo, recipe section, and arch doc cascaded by any ✅ Decision in this file + every "**Locked**" / "**Decision**" / "cascaded into" line in `training_recipe.md §1–§9` + every "amended" / "supersedes" line across the canonical-source memos (`project_v14_arch_post_v3_amendment_2026_05_19`, `project_v14_imindbench_multistft_pivot_2026_05_22`, `project_v14_three_phase_staged_recipe_2026_05_18`, `project_v14_cross_subject_pretraining_data_strategy_2026_05_22`).
- **Current state**: 13 ✅ decisions retained in this doc + 26 locks merged into recipe §9 + 8 REFINE-recipe-additions + 12 BIG / 30 MED / 2 SMALL open items still to settle. The doc-side ✅ is **not bit-equivalent** to "code-side implements the decision." Decisions land as text in this doc + a one-line "cascade" claim in the recipe memo; the actual extractor / model / loss / sampler / experiment-config modules may still hold pre-decision defaults — and there is no automated check that the cascade reached code.
- **Gap (silent-propagation failure mode — the failure mode this blocker exists to prevent)**:
  1. A v14 blocker resolved in this doc looks **identical** (from the doc) to one that was also implemented in code.
  2. Successive amendments (v3 → v4 → 5/22 pivot → 5/23 batch) silently overwrite each other's cascade obligations; the most recent doc-edit wins on paper, but the code may still reflect an intermediate state from a prior amendment.
  3. When all 207 open items finally clear and Phase-1 dispatch launches, the submitter silently inherits the union of (a) stale defaults nobody updated post-decision and (b) inter-amendment drift between locks. **This is invisible until Phase-1 loss curves look wrong** — at which point ~9 weeks of compute is sunk and the diagnostic surface is the entire 244-item decision history × every file in `src/speech_decoding/`.
  4. The failure mode is *structurally undetectable* by per-blocker walkthrough because each individual blocker, viewed in isolation, says "✅ Decision: X, cascaded into Y." No single blocker owns the cross-check.
- **Default candidate (audit protocol)**:
  1. **Build the closure ledger** (`reports/v14_closure_audit_<date>/decisions.csv`): parse three sources — (a) ✅ Decisions in this doc, (b) **Locked** entries in `training_recipe.md §9`, (c) the "What's locked" section at the bottom of this doc (architectural defaults that never went through a blocker). One row per *sub-claim*, not per blocker (B02 expands to ~10 rows; B08 to 3; B01 to ~8). Schema: `(decision_id, sub_claim_id, decision_date, target_files, expected_observable_state, source_line)`. Expected_observable_state is a grep-able / read-able assertion (e.g. `B01.opt → betas=(0.9, 0.95), weight_decay=0.05` in `experiments/v14_phase1.py`; `B08.mask_shape → keep_block_t=5, keep_block_f=6` in the Phase-1 masker; `B11.K → layer_avg_K=6` in the loss). For ✅ decisions written before the `Verifiable as:` convention landed, backfill the assertion column by reading each decision's "Decision" line and producing a grep target (this is part of step 1 work, not a separate phase).
  2. **Ledger-completeness assertion (audit-of-the-audit)**: before running per-row verification, assert `len(ledger_blockers) == count(✅ in this doc) + count("Locked" in recipe §9) + count(items in "What's locked" section)`. Mismatch → ledger silently misses decisions → audit cannot be trusted → halt and reconcile before proceeding. Without this step the audit's "all green" verdict is non-load-bearing.
  3. **Verify each row mechanically**: for each row, open the cited target file(s) and assert the expected state is present. Record verdict `pass / drift / pending-impl`. Drift = code disagrees with decision. Pending-impl = decision references a target file/symbol that doesn't yet exist (legitimate if the corresponding module is unbuilt). **Pending-impl aging policy**: every `pending-impl` row must be backed by an open blocker (B-block / IE / DP / NT) whose closure will build the target; if no open blocker owns it, auto-promote to `drift`. Pending-impl is the audit's amnesia bucket — without aging it grows forever and silently hides drift.
  4. **Three independent subagents as cross-validation** (one per lens — code-correctness, memo-cascade-consistency, paper-defensibility). Each writes per-row verdicts. Disagreements escalate to Ben before continuing.
  5. **Output `reports/v14_closure_audit_<date>/audit.md`**: per-decision verdict matrix + flat `drift` list + flat `pending-impl` list + recommended fix per drift row + `last_decision_timestamp_covered` field = max `Decision (date)` across the ledger. Any `drift` row that cannot be fixed in <2 h becomes a new B-block (e.g. `B-drift-<orig_id>`) reopening the original blocker with explicit "code-vs-decision mismatch" framing.
  6. **Hard-gate `scripts/dcc/dispatch` on the audit, via TST10's pytest mechanism (compose, don't parallel)**: add `tests/test_closure_audit_green.py` marked `must_pass_before_dispatch`. The test (a) reads the latest audit report, (b) asserts zero `drift` rows, (c) asserts `last_decision_timestamp_covered >= max(Decision date)` across this doc + recipe §9 — guards the stale-audit race where Ben closes a blocker after the audit run and dispatches before re-running. Re-using TST10's gate avoids a second independent pre-dispatch hook to keep working.
  7. **Update CLAUDE.md "Sweep discipline" section** with a single line: "No Phase-1 / Phase-2 / Phase-3 dispatch without a passing closure-gate audit report under `reports/v14_closure_audit_<latest>/audit.md`."
- **What this audit is NOT**:
  - Not a per-blocker re-litigation. Decisions stay decided. The audit checks **implementation faithfulness to the decision**, not whether the decision was right.
  - Not a check on undocumented in-conversation decisions. Anything decided in a chat that never made it into this doc, recipe §9, or a canonical memo is **out of scope** — invisible to the audit by construction. Implication: every meaningful decision must round-trip through this doc (or recipe §9) before it counts. "We talked about it on Tuesday" is not a decision the audit recognizes.
- **ML precedent**: not an ML precedent — software release-engineering practice. Closest analogues: ADR (Architectural Decision Record) compliance audits, infra change-management drift checks (Terraform `plan` vs state), spec-implementation traceability matrices used in safety-critical software (DO-178C objective satisfaction reviews).
- **Settle-by**: **AFTER** every other open BIG + MEDIUM blocker is ✅, **BEFORE** Phase-1 dispatch. This is the **closure gate** — it cannot run earlier (nothing to audit) and must not be skipped later (the failure mode it prevents is the reason it exists). Skipping it because "every blocker is closed so we must be fine" is exactly the silent-propagation trap.

### B19. ✅ P1/P2 loss-design lock (2026-05-24; amended 2026-05-25 by B21/B22/B03 bundle)
> **Canonical superseding form** is the 5-term post-B22 + B03f objective in `memory/project_v14_b03_mask_lock_2026_05_25.md`. The 3-term ASCII below is the historical B19 snapshot; reading order: B19 (this entry) → B21 (collapse-prevention) → B22 (M3 dense supervision) → B03 bundle (mask discipline + supervision-set scope).

- **Headline**: P1 and P2 share a single bootstrap mask-prediction objective in one student forward; full ❶❷❺❻ stack trains both phases.

  ```
  Historical 3-term (B19, 5/24):
  L_total = L_pre_frame@M2  +  L_post_frame@M4  +  1.0·L_post_utterance@M4-PMA

  Canonical 5-term (post-B22 + B03f + B03 PM revert):
  L_total = L_pre_frame@M2 + L_mid_slot@LN_mid(M3) + L_post_frame@LN_frame(M4)
          + 1.0·L_post_utterance@LN_utt(M4)-PMA + 0.1·L_DKoleo@M4
  ```

- **Loss family**: bootstrap mask-prediction, EMA teacher (linear-ramp momentum τ → 1.0), stop-grad on teacher path. Teacher target = all-N/L-layer-averaged with per-layer instance-norm (EAT §3.1 + data2vec 2.0). MSE loss. Fixed coefficients (1, 1, 1.0) on B19 terms; (1, 1, 1, 1.0, 0.1) on canonical 5-term. Joint from step 1, no schedule. No predictor on utterance head (EAT §3.1). Single student forward, shared input-patch mask feeds all losses.
- **Phase 1**: cross-attn ❺ + latent stack ❻ ON, anatomy bias OFF (`bias_enabled=False` buffer; flips True at P2 init). Full stack trains (~15M params, was 6.6M Stage-A-only pre-B19). SWEC reaches ❺ with bias globally zeroed (no per-sample anatomy lookup).
- **Supervision-set gates** (post-B03f): `parcels_supervised[subject]` (per-subject DK parcels with ≥1 electrode in anatomy, computed ONCE at extractor time, invariant to within-clip masks). Gates `L_mid_slot` + `L_post_frame` divisor + `L_post_utterance` PMA softmax (additive `MASK_NEG=-1e4` per B12). Shaft-orphaned parcels REMAIN supervised — that IS the JEPA training signal. SWEC fallback: `parcels_supervised[subject] = ∅` (no-op). Slot-bank regularizers (`L_DKoleo`, monitor F1, reactive Gram) operate over all 320 slots per B21 default (NOT `supervised_idx` — B03 PM revert).
- **Per-electrode mask discipline**: ≤ 50% per electrode (HARD cap, was 60–65% pre-B19). No whole-shaft / whole-electrode in P1 (reserved for P2's shaft-block layer per EX03). Patch mask substitution = paradigm B drop + 2-block predictor warm-started from P1 ckpt at P2 step 0 (B03c).
- **PMA-k=1 over parcels**: trained by `L_post_utterance` in P1+P2; reused at P3 (cross-modal) and P4 (probe, frozen per B07/ARG03). Pools parcel axis (320→1) → mean-pools time (T→1).
- **Phase 2**: layers shaft-block electrode mask (EX03; B03 drop semantic; **K-cap revised twice 2026-05-27**: AM K=3→K≤2 fraction-based; **PM K≤2→K=1 default**: `K = 1 if N_shafts ≥ 2 else 0`, block α only, ~12.5% effective on BT 8-shaft median; combined with 50% patch mask = ~44% visible tokens, centered in biosignal-FM band) on top of patch mask. Stage-A-frozen-during-P2 lock RETIRED — P2 is full-stack fine-tuning of P1 ckpt with bias-on. EMA teacher sees FULL electrodes + FULL patches (B03d asymmetry). Sisters `R-shaft-K2` (P0 falsifier) + `R-shaft-K3-mixed-3block` (P1) + `R-stratified-shaft-mask` (P0 mean-collapse mitigation) settle the K-cap empirically.
- **Phase 3**: unchanged — drops B19 objective, swaps to single Smooth-L1 vs Whisper-L8.
- **Compute re-cost** (5/25 supersession): P1 actually ~600 H100-h median (was 2.8e21 / 12–14d / $12–14k v3-era estimate before v4 invisible-frontend lock collapsed token count 10×). See HB02 closure + memo §POST-V4 RE-COST.
- **Sister cells** (P0, `ablations.md §6L`): `BiL-B22-Default` (5-term headline; replaces retired `BiL-Loss-Default`), `BiL-Loss-NoUtt`, `BiL-Loss-NoPre`, `BiL-Loss-LambdaSweep` (λ_utt ∈ {0.1, 0.5, 1.0, 2.0, 5.0}), `R-p1-stage-a-only` (pre-B19 reversal), `R-p2-no-patch-mask`. P1 conditional: `BiL-Loss-AddGram-P3`, `BiL-Loss-L1`. (`BiL-Loss-AddKoLeo` retired — DKoleo in default per B22.)
- **Cascade**: `memory/project_v14_loss_design_lock_2026_05_24.md` canonical for B19 snapshot; `memory/project_v14_b03_mask_lock_2026_05_25.md` canonical for 5-term form; staged-recipe memo top-of-file 5/24 amendment block; training_recipe.md §1/§2/§3/§4/§7/§8/§9; plan.md §"SSL recipe" P1/P2.
- **Verifiable as**:
  - `L_total = L_pre_frame + L_mid_slot + L_post_frame + 1.0 * L_post_utterance + 0.1 * L_DKoleo` in `src/speech_decoding/experiments/v14_phase{1,2}.py`
  - `bias_enabled=False` initial value on cross-attn buffer in `models/v14_encoder.py`; flips True at P2 init
  - `parcels_supervised[subject]` build-time path in `extractors/parcel.py` (per B03f); grep `valid_parcels\[clip\]` returns 0 hits in active loss sites
  - Per-electrode mask sampling: hard clip at 0.50 in mask-sampler module
  - PMA path: `PMA(M4, query=q, axis=parcel)` → `mean(., dim=T)`; no predictor between student and teacher utterance vectors
  - DKoleo + monitor F1 + reactive Gram operands grep returns 0 hits on `supervised_idx` indexing (all 320 slots)

---

## MEDIUM gaps (have a reasonable default, should be stated)

### M02. UFO frame objective — granularity
- ✅ **Decision (2026-05-26, retired as obsolete vocabulary)**: M02 closed. "UFO frame" was pre-B19 terminology that no longer maps to a distinct loss term. Per-patch frame supervision is owned by B19's `L_pre_frame @ M2` (granularity = per-(electrode, F_p=10 × T_p patch grid) post B20 v4 Conv2d (3,2)); per-(parcel, t) frame supervision is owned by B19's `L_post_frame @ M4`; mid-stack supervision is owned by B22's `L_mid_slot @ LN_mid(M3)`. B11's K=6 layer-averaging is a *target-construction* recipe (per-layer instance-norm + average over all N/L teacher layers), not a competing supervision term — the 5/23 red-team redundancy concern dissolved when B19+B20 made the patch unit explicit. No code or recipe change required.
- **Verifiable as**: grep `UFO.*frame|ufo_frame` returns 0 hits in `src/speech_decoding/`, `docs/neuroprobe/training_recipe.md`, and active memos (historical mentions in archived memos OK).

### M06. ✅ Phase 2 corpus batch composition (2026-05-23, expanded same-day PM)
- ✅ **Uniform-per-subject across 94 sEEG subjects (D 85 + BT 9)**: D 90.4% / BT 9.6% (85:9 subject ratio). Was D 52.6% / BT 47.4% before audit corrected the stale "10 D-pts" cite. Per-row weight = `1 / (N_subjects_global × clips_per_subject[subject_id])`. Rationale: P2 trains cross-subject parcel routing under shaft-mask — unit-of-learning is **one subject's anatomy/coverage pattern**, not hours. Hour-weighting would let a 30h D-pt contribute 6× more routing signal than a 5h BT-pt (backwards for cross-subject generalization). Different sampler unit than P1 (B02 hierarchical α=0.5 over vb-eh). BT share drop 47.4%→9.6% load-bearing; Ben accepted 5/23 PM.
- **Sisters**: `R-p2-pooled-h` (hour-weighted alt), `R-p2-uniform-session` (session-level granularity), `R-p2-bt-share-cap-50` (cap BT share at 50% — falsifier), `R-p2a-bias-off-pretrain` (preserves the rejected 2a/2b sub-stage as a sister).
- Audit memo: `[[project_d_cohort_phase2_cohort_audit_2026_05_23]]`.
- **Verifiable as**: P2 sampler in `experiments/v14_phase2.py` uses `weights = 1.0 / (n_subjects * clips_per_subject)`; `n_subjects == 94`; BT effective share assertion in 9–11% band at sampler-build.

### M07. ✅ SSL train/val split P1+P2 (2026-05-26, joint S06)
- ✅ **Session-held-out val.** Hold 1 whole session/subject for SSL val (BT: 1 film, SWEC: 1 night, D: 1 session, AJILE12: 1 day). Held-out sessions share no montage / drift / film-content with train → val L_recon is honest generalization signal, not leakage-inflated within-session proxy. Fallback for subjects with only 1 session: 5% clip-level stratified hold. SSL val L_recon is **secondary** monitor only — S06 probe r² is the primary best-checkpoint criterion. Session list serialized at sampler-build for DDP determinism.
- **Verifiable as**: `val_session_ids: dict[subject_id, list[session_id]]` precomputed in `extractors/` and loaded by `V14Data`; `assert train_session_ids[s] ∩ val_session_ids[s] == ∅` for every subject; 1-session-fallback path logs per-subject warning.

### M08. ✅ Phase 3 train/val split + early-stopping (2026-05-26)
- ✅ **Honor B01 v3 lock: 5% clips-held-aside per film, session-stratified, patience 5 epochs, eval ~1k steps.** Film-held-out (M07 analogue for P3) **rejected on data-loss grounds**: holding 1 film/BT-subject = 25–35% P3 data loss on ~200h paired corpus; 5% clip-stratified costs ~2–3% and keeps film distributions in train. Whisper-L8 val/train representation overlap IS real but no longer load-bearing — S06 probe-r² (2026-05-26 lock) is the primary best-checkpoint criterion; Smooth-L1 Δ is secondary. Prior "90/10 within-subject + patience 3" default dropped.
- **Sister `R-p3-film-heldout`** (`ablations.md`): held-out-film alt for attribution if Whisper-L8-overlap surfaces in P4 results.
- **Verifiable as**: P3b config in `experiments/v14_phase3.py` sets `val_split="clip_stratified_per_film"`, `val_clip_frac=0.05`, `early_stop_patience_epochs=5`, `eval_steps=1000`.

### M10. Per-session normalization stat computation path
- **Where**: arch memo §12 — robust-z per (electrode, freq-bin, session).
- **Gap**: precomputed offline (per-session JSON cache) or online (running estimate during training)? Affects whether new sessions can be added without recompute.
- **Default candidate**: precompute offline per session, cache to disk, lookup at training time. Recompute when adding new sessions.
- **🚩 FLAGGED (2026-05-23, 4-agent merge audit)** — Red-team: M10 default is silent about the held-out CSubject eval case (no train-side per-subject stats exist for an unseen subject). Engineer reading M10 in isolation may default to per-session stats *computed from the test session itself* — stats-side leakage (separate from label leakage but still inflates CSubject AUROC by ~0.01–0.02, enough to phantom-pass the 0.628 gate). Settle: M10 default explicitly references the B13 two-scope policy (CSession = per-(subject, train-session); CSubject = pooled stats over training cohort). Recipe §2 now has the partial fix; ensure M10 closure cross-refs B13.

### M17. ✅ Cross-corpus session-boundary handling (2026-05-26)
- ✅ **Session = BIDS-`session` field per corpus, no subdivision.** BT 1 film, SWEC 1 multi-day recording (24h+, mixed sleep/wake), AJILE12 1 day, D 1 clinical session. Clip extraction respects this boundary (no cross-session clips). SWEC's mixed-state heterogeneity propagates as a *data property* — SSL learns to average across states at the frame level (right inductive bias for non-stationary clinical FM). Robust-z keyed per (subject, BIDS-session, electrode, freq-bin) per B13. Consistent with M07.
- **Trade**: P2 cross-attn routing under shaft-mask MAY overfit to per-state electrode-coverage drift within a SWEC multi-day session. Mitigation: B22 collapse-prevention (DKoleo + identity init). Sister `R-swec-session-subdivide-2h` (P2) tests continuous-2h-block subdivision if P2 cross-subject probe r² stalls.
- **Verifiable as**: `session_id = bids_session_field` in every corpus extractor; `assert clip_start_session == clip_end_session` for every emitted clip; `R-swec-session-subdivide-2h` config in `ablations.md`.

### M18. ✅ Clip extraction strategy (2026-05-26, coupled to B02)
- ✅ **Honor B02 WRS (with-replacement); drop "20× per epoch" scalar.**
  1. **Clip-position grid**: random 5s offset within session, continuous time-grid (NOT pre-discretized cache). `start_time ~ Uniform(0, session_duration − 5s)`. Deterministic per-epoch via B02's per-rank `Generator().manual_seed(base_seed + rank + epoch)`.
  2. **Sampler**: B02's `WeightedRandomSampler(replacement=True)` over vb-eh weights, wrapped by TorchData `StatefulDataLoader`. No B02 modification.
  3. **Per-corpus repeat rate emergent**: BT (~30k offsets at 1Hz, 7.3% draws) → ~10× repeats/offset over 4.1e8 draws; SWEC (~6.7M, 50%) → ~3× per offset. Imbalance is the *intended* expression of corpus weighting, not a bug to flatten.
  4. **"20× per epoch" scalar dropped**: undefended, conflicted with B02's `num_samples` lock.
- **Verifiable as**: `start_time = rng.uniform(0, session_duration - 5.0)` in clip-extraction site of `extractors/`; grep `20.*epoch|num_clips_per_session` returns 0 hits as baked scalar; B02 WRS unchanged.
- **⚠️ AMENDED for SWEC (2026-05-31, DP03 contract, Ben):** SWEC **overrides** the random-continuous-offset rule above → **non-overlapping stride-5 s tile grid** per part (chosen for leakage-cleanliness: exact corpus count + whole-tile held-out, no train/val partial-overlap). M18 stays the rule for **broadband** (BT/AJILE12/D). B02 mechanism unchanged — only SWEC's vb-eh weight switches from repeat-rate estimate to *exact* tile-count `Σ_part floor(part_dur/5 s) − guarded tiles` (a DP02-measurement detail). WRS-with-replacement retained (non-overlap = property of the *pool*, not the draw). Falsifier sister **`R-swec-wrs-sampler`** reverts SWEC to this M18 sampler. Full rationale: `docs/neuroprobe/dp03_continuous_corpus_contract.md` §2 row 3.

### M19. ✅ Phase-3 preflight subject choice — retired post-B06 (2026-05-26)
- ✅ **Retired as moot.** B06 (5/25 PM) retired the P3 ridge preflight entirely; teacher rate=8 Hz + layer=Whisper-L8 are first-principles locks with falsification by `R-rate-{5,10,16}Hz` + `R-layer-{6,10}` sisters running on full P3 corpus (all 9 BT subjects). No preflight → no subject-choice decision.
- **Verifiable as**: grep `preflight.*subject|ridge.*sweep.*phase3` returns 0 hits in `experiments/v14_phase3.py`; `teacher_rate_hz = 8` + `teacher_layer = 8` locked; sister configs use `subjects=ALL_BT`.

### M28. Per-corpus notch filter frequency
- **Where**: recipe memo §2 line 63 — `iircomb @ 60 Hz` applied uniformly.
- **Current state**: single 60 Hz notch applied to all corpora.
- **Gap**: SWEC is Switzerland (50 Hz line noise); AJILE12 / D-cohort / BT are US (60 Hz). The fixed 60 Hz notch under-suppresses SWEC's 50 Hz contamination, then the broadband 30-bin filterbank silently absorbs the residual.
- **Default candidate**: per-corpus notch parameter — SWEC = 50 Hz, AJILE12 / D / BT = 60 Hz. Implement as per-corpus extractor argument on the iircomb call. Cheap fix, no downstream re-architecture.
- **ML precedent**: standard biosignal preprocessing (mne, EEGLAB) parameterizes line frequency per dataset.
- **Settle-by**: pre-Phase-1 dispatch.

---

## SMALL gaps (implementation defaults; low risk if standard)

### S06. ✅ Checkpoint cadence + best-val criterion (2026-05-26, joint M07)
- ✅ **Every 5k steps; best-3 by downstream probe r²; co-save EMA teacher state.**
  1. **Cadence**: 5k steps (P1 = 80 over 400k; P2 = 8 over 40k; P3 = ~4 over 20k). Keep best-3 + latest + every-50k-anchor.
  2. **Primary criterion = downstream probe r²** on tiny held-out Neuroprobe task at every 5k save. Default: linear logistic regression on `speech-onset binary` (sub-1, M07 session-held-out, 5-fold CV) from frozen `M4` mean-pooled-T. ~3 min/probe → ~0.5% wall overhead. **Collapse-immune**: collapsed M4 → probe r²→0 even as L_recon→0. Secondary: M07 val L_recon (logged not selected). Tertiary: B21 collapse monitor (rank, DKoleo, F1 over parcel-ID probe).
  3. **Co-save EMA teacher** in every checkpoint (data2vec 2.0 / V-JEPA precedent — teacher is part of optim state, resume without it spikes loss ~5k steps).
- Probe-task rationale: speech-onset in-distribution for BT P1, doesn't leak P4 eval tasks, low cross-cohort variance.
- Sister `R-bestval-emaval` (P1): selects by EMA val L_recon as falsifier.
- **Verifiable as**: `ProbeTask(name="speech_onset_binary", subject="sub-1", val_split="m07_session_held_out")` callback every 5k in `experiments/v14_phase1.py`; `assert "ema_teacher" in ckpt.keys()`; checkpoint pruning by probe-r² desc, keep top-3 + latest + multiples of 50k.

### S10. BT session-boundary preservation in batches
- **Where**: not specified.
- **Default candidate**: session-aware shuffling (each batch can mix sessions but clips within a sample come from a single session — preserves Nv14 consistency).
- **🚩 FLAGGED (2026-05-23, 4-agent merge audit)** — Red-team: per-session electrode count varies (one session 80 contacts, another 200), so the per-sample C_MAX padding mask differs per row. Row-mean loss reduction (locked by B02) handles it correctly. **BUT**: PMA k=1 mean over parcels may not honor the per-sample parcel-validity mask — if some rows have zero electrodes contributing to a parcel and others don't, the PMA pool silently averages garbage. Verify per-sample parcel-validity mask is row-aware in the PMA reduction before P2 dispatch.

---

## Repatriated open items (from archive — abbreviated)

These are items the discovery audit surfaced that were silently archived during the 2026-05-23 88% line-reduction pass and walked back in on 2026-05-23 PM after a 4-way audit (lens-blind self-pass + 3 lens-specific agents: pre-dispatch-correctness, paper-defensibility, silent-failure). Each entry is a pointer; full rationale + decision candidate lives in `docs/archive/v14_blockers_audit_trail_2026_05_22.md` under the matching ID. Severity tags retained from archive.

### Pre-Phase-1 unit tests (TST)

- **TST01** (BIG): pytest `must_pass_before_dispatch` marker + CI gate wiring.
- **TST02** (MED): multi-corpus sampler share + per-rank DDP + persistent-worker + resume round-trip test for B02 lock.
- **TST03** (BIG): P1↔P2 checkpoint strict-mode test (companion to RT10).
- **TST04** (MED): EMA teacher `_step` counter survives checkpoint round-trip.
- **TST05** (BIG): P1 loss NaN detector under bf16 Multi-STFT + log + robust-z (companion to RT01).
- **TST06** (MED): Whisper-L8 `(50, 1280)` shape contract test.
- **TST07** (MED): cross-attn invalid-parcel zero-gradient assert (companion to IE08).
- **TST08** (MED): triangular-pool determinism + sum-to-1 weights test (companion to B05).
- **TST09** (MED): random-label memorization canary at Phase-4.
- **TST10** (BIG): `scripts/dcc/dispatch` runs `pytest -m must_pass_before_dispatch` before sync.
- **TST11** (MED): Phase-resume bitwise determinism (fresh vs checkpoint-resume).

### Silent-failure traps (RT)

- **RT01** (MED): bf16 `log(0+eps) → -inf` cascade; lock `log_eps = 1e-4`. B12 covers attn sentinels only, not log.
- **RT03** (MED): Phase-1 mask-rate off-by-one detection (all-masked input → chance-loss assert).
- **RT07** (MED): DK index-map 0/1-indexed convention pin + `vocab_version` cache field.
- **RT08** (SMALL): FreeSurfer version drift across subjects → anatomy label remap.
- **RT10** (BIG): Phase-boundary checkpoint `strict=False` silently random-inits; lock `strict=True` at boundaries.
- **RT17** (SMALL): within-epoch subject ordering biases EMA teacher state; shuffle per epoch.

### Reproducibility plumbing (CR)

- **CR01** (MED): DDP needs explicit `torch.cuda.manual_seed_all(seed)` after PL `seed_everything`.
- **CR02** (MED): DataLoader `worker_init_fn` explicit seeding.
- **CR04** (MED): Whisper-L8 feature cache needs Whisper-model-version hash for invalidation.
- **CR06** (SMALL): Phase-4 per-task probe init re-seed with `seed + task_idx`.
- **CR07** (SMALL): bf16 cross-hardware non-bit-exact → exca cache invalidation ops note.
- **CR08** (SMALL): Phase-3 grad-accum order + single-scalar `.backward()`.

### Implementation gaps (IE / DP / NT)

- **IE03** (MED): EMA teacher in P2 with frozen Stage A — explicit spec (Stage A constant copy, Stage B EMA-updated).
- **IE04** (MED): valid-bin mask loss-side per-sample reduction implementation (avoids cross-corpus gradient-magnitude bias).
- **IE08** (MED): cross-attn invalid-parcel gradient-flow invariant; test = TST07.
- **DP01** (BIG): variable-T RoPE buffer + variable-C `collate_fn` across phases (partly resolved by ARG04 5 s lock; variable-C still open).
- **DP03** (BIG): scaffold `SWECStudy` / `DCohortStudy` / `AJILE12Study` NeuralFetch class subclasses. **✅ CONTRACT-LOCKED for SWEC (+ AJILE12 shape) 2026-05-31** → `docs/neuroprobe/dp03_continuous_corpus_contract.md` (loader method-by-method, seizure guard `[−30,+90]`, native-CMR accept, resample 512/1024→2048, M18 SWEC-override to non-overlap tiling). **Still open**: DCohort loader + AJILE12 CONFIRM-cells (contract §5) + the actual implementation (the `_load_raw` part-file memory strategy, contract §7.1).
- **DP05** (MED): per-subject DK index-map serialization to disk cache.
- **DP06** (MED): session-stratified clip extraction respecting session boundaries. **✅ CLOSED 2026-05-31** — realized by M17 (session boundary) + M18 (sampling, SWEC-override) + DP03 contract §3 (`_load_timeline_events` = non-overlap tile grid minus seizure-guard windows, per-session 5%-stratified held-out per M07).
- **DP08** (MED): BT multi-session subjects — per-session robust-z stat keying with global EMA teacher.
- **DP13** (MED): switch dispatch default from `LogStftView` → `MultiStftView` + integration test.
- **NT01** (BIG): single `V14Experiment` with `phase: Literal[1,2,3a,3b,4]` parametrization.
- **NT02** (BIG): `V14Data` with phase + corpus_mix + sampler_spec parametrization.
- **NT03** (MED): exca cache subdirectory layout per phase + grid cell.
- **NT04** (MED): explicit `submit_phase_<N>.py` per dispatch target (TaskInfra vs MapInfra mapping).
- **NT08** (MED): Multi-STFT extractor precompute as `MapInfra` job + cache-key spec.
- **NT09** (MED): checkpoints to `/hpc/.../cache_neuroai/` (persistent), NEVER `/work` (75-day purge).
- **NT11** (MED): Phase-3 preflight result JSON contract for Phase-3 full-dispatch handoff.
- **NT13** (MED): per-phase collector script paired with each `submit_*` (CLAUDE.md "no sweep without collector").

### iMINDBench parity / spec divergence (IM)

- **IM01** (MED): verify iMINDBench "Global Norm" ≡ v14 Nv14 robust-z (cheap paper-side read).
- **IM04** (BIG): iMINDBench "flatten" — really flatten, or pool-then-flatten? Re-verify before claiming protocol match.
- **IM05** (BIG): iMINDBench logistic baseline is raw-spectral not Whisper-aligned → pre-bake `R-imindbench-raw-logistic-parity` sister.
- **IM06** (SMALL): per-corpus valid-bin mask vs iMINDBench all-bins training; `R-no-valid-mask` sister.
- **IM08** (MED): Laplacian (iMINDBench) vs shaft-CAR (v14) re-reference; parity sister uses Laplacian.
- **IM11** ✅ **(2026-05-26)**: Phase-1 high-bin (k22–k29) under-training accepted as the intended cost of B02's α=0.5 quality-weighted sampler. Mechanism documented: SWEC valid k0–k21 (Nyquist 250 Hz, 0.5–120 Hz bandpass per Charmander) + AJILE12 valid k0–k20 (500 Hz / 0.5–200 Hz, Peterson 2022) contribute zero gradient to bins k22–k29; only BT (7.3%) + D-cohort (15%) train high bins = 22.3% effective high-bin gradient share. Phase-2's 224h sEEG-only corpus (D-cohort 90.4% + BT 9.6% per M06) IS full-bandwidth, so P2 cross-attn does see high-bin training on the 224h tune — IM11 is "sparser P1 pretrain on high bins" not "no training." **Falsifier pre-registered**: `R-sampler-broadband-uniform` (B02's existing sister: SWEC 30 / AJILE 30 / D 25 / BT 15 macro split → ~40% high-bin share) escalates IF Phase-2 + Phase-4 results show high-bin-discriminative-task underperformance (high-gamma tasks in Neuroprobe). Honest framing: P1 is field-potential SSL on a primarily-broadband corpus mix; high-band is the genuinely under-represented modality, and the sampler shouldn't be retroactively reweighted to compensate for what's actually in the data. **Verifiable as**: grep `bin_reweight|high_bin_loss_scale` returns 0 hits in `src/speech_decoding/experiments/`; `R-sampler-broadband-uniform` sister config exists in `ablations.md §6j`; B02 α=0.5 hierarchical lock unchanged.
- **IM13** (CLOSED 2026-05-25): magnitude vs log-power inconsistency resolved by flipping default to raw filterbank magnitude (no log on the value axis). See `memory/project_v14_stft_abs_default_2026_05_25.md`. F-log-amplitude sister keeps the log variant accessible via `apply_log=True`.

### Phase-4 per-task contracts (PT)

- **PT01** (BIG): per-task eval window — locked at [0, 1] s slice from 5 s clip or task-specific?
- **PT02** (MED): class-weighting policy for per-task linear probe (match baseline).
- **PT06** (MED): probe regularization (AdamW wd=0.0 vs sklearn C=1.0) — match baseline OR sweep.
- **PT07** (MED): Phase-3 preflight scope — speech-cortex r² may underfit non-speech tasks (per-task layer sweep).
- **PT10** (MED): label-timing semantics per task vs flat-window assumption.
- **PT11** (SMALL): per-task stimulus-overlap flag — filter or diagnostic?
- **PT12** (MED): re-run Linear-Lap+spec baseline on v14's exact pipeline (paper number may not transfer).
- **PT15** (SMALL): sample-level vs trial-level aggregation in Phase-4 metrics.

### Paper rigor / evaluation (EV)

- **EV01** (MED): baseline CI on submission gate — 5-seed CNN-Lap / Linear-Lap re-run with bootstrap CI bands.
- **EV02** (MED): SSL phase-chain seed granularity (N=1 tower vs N≥3 anchor stage).
- **EV06** (MED): Phase-4 readout split-parity — rerun reference impls on identical splits; do not cite paper numbers as direct comparators.
- **EV07** (MED): headline-gate statistical test — bootstrap CI + paired Wilcoxon spec.
- **EV08** (MED): "≥ 4 tasks" gate set — pre-publish exact task list in plan.md.
- **EV09** (SMALL): dual-prong CSession × CSubject independence check on baselines (Pearson ρ).
- **EV13** (MED): report Path-A frozen-linear (headline) AND Path-C light-fine-tune ceiling for "realistic deployment" comparator.

### Paper framing / reviewer rebuttal (PF)

- **PF01** (BIG): explicit "anatomy ≠ per-subject params" rebuttal text + AB14 sister reported in main body.
- **PF03** (MED): Whisper-L8 cross-modal novelty framing vs Goldstein / Antonello / Shimizu / BrainWhisperer (cohort scale + routing).
- **PF04** (BIG): §Comparison-table-by-regime distinguishing within-session (DIVER-1 0.678) vs cross-session vs cross-subject; pairs with BP02.
- **PF05** (MED): iMINDBench preprint circularity defense (front-end lift + Multi-STFT extension + F-CQT / F-single-STFT sisters).
- **PF06** (MED): 9-subject cohort defense (Neuroprobe benchmark-bounded + Bhattacharjee 8-subj precedent).
- **PF08** (MED): AJILE12 Phase-2 exclusion topology-mismatch framing.
- **PF09** (MED): submission-gate ±0.05 justification (field-convention cite + dual-prong + ≥ 4-task triangulation).
- **PF11** (MED): post-hoc SRM (Bhattacharjee 2025) baseline sister `R-posthoc-srm` for "linear-shared-space could've done this" rebuttal.
- **PF12** (SMALL): "zero per-subject params" linguistic precision footnote (output IS subject-specific via routing).
- **PF13** (SMALL): frozen anatomy "why not learn the map?" rebuttal + X3 learned-correction sister.
- **PF14** (SMALL): Whisper-L8 = acoustic-phonetic NOT semantic — defensive citation (Goldstein 2025 + Antonello 2023).
- **PF15** (SMALL): EMA-teacher latent prediction ≠ BERT-MLM clarification (cite data2vec 2.0).

### Baseline parity (BP)

- **BP01** (BIG): PopT data-scale + param-count parity — fine-tune PopT on v14 pipeline OR honest disclosure.
- **BP02** (BIG): drop DIVER-1 from headline-gate comparator; move to §Within-session-ceilings table.
- **BP10** (MED): for each baseline report both frozen + fine-tuned variants; gate uses max.
- **BP11** (MED): standardize gate metric (balanced-accuracy multiclass + AUROC binary).
- **BP14** (MED): lock v14 CrossSubject = strict LOSO; verify each baseline's CSubject definition.
- **BP16** (SMALL): pre-bake ≥ 30M v14 sister to defend "wins at 13M" against PopT 80M.
- **BP20** ✅ **(2026-05-26)**: Phase-1 BT-subject overlap with P4 Neuroprobe eval handled per field-standard iEEG-FM convention (PopT ICLR 2025, BarISTA NeurIPS 2025, BrainBERT, MVPFormer): SSL pretrain sees ALL 9 BT subjects; cross-subject evaluation is leave-2-subjects-out at the **linear-probe stage**, NOT at the SSL stage. The pretrained encoder is general-purpose; the readout is what's held out. **Three locks**: (a) Clip-level overlap audit (~1 hr task pre-Phase-1): P1 uses random 5s offsets, P4 uses fixed trial-aligned windows; collision rate mathematically ~0% but verify with one manifest grep. (b) CSubject probe split = strict leave-2-subjects-out with no-electrode-overlap between probe-train and probe-test (enforced in `experiments/v14_phase4.py` collator). (c) Methods paragraph (Ben drafts per IRONCLAD) cites the 4-paper convention + states the leave-2-subjects-out probe stringency. **Sister `R-bt-held-out-in-phase1` pre-registered as REVIEWER-REBUTTAL cell**, NOT a default ablation — runs ~600 H100-h only if a reviewer asks for the held-out-from-SSL falsifier. No default-path P1 cost. **Verifiable as**: manifest-overlap audit report at `reports/v14_bp20_clip_overlap_audit_<date>/`; CSubject probe split assertion `assert set(probe_train_electrodes) & set(probe_test_electrodes) == ∅` in `src/speech_decoding/experiments/v14_phase4.py`; methods paragraph in `paper/neuroprobe-hillclimb/methods/`; `R-bt-held-out-in-phase1` sister config tagged as `tier=rebuttal` in `ablations.md`.
- **BP21** (SMALL): lock iMINDBench baseline = Multi-STFT-Logistic 0.663 (matches v14 front-end).
- **BP22** (MED): DIVER-1 domain-match — `R-bt-only-phase1` sister to test "domain > scale".

### Sister-cell reviewer defense (AB / EX)

- **AB05** (MED): latent-index permutation test (does the index carry routing info, or does the bias do all the work?).
- **AB08** (MED): latent-stack parcel-SA bias sweep — no bias / T5-learnable / BNA-connectivity-frozen (settles B09).
- **AB14** (MED): BNA-soft-native sister — falsify DK-hard vs BNA-soft routing without MNI parity.
- **AB15** (MED): compute-matched baseline (~13M ViT-B-8-layer) to defend "wins are architectural not flop-spend".
- **EX06** (MED): Whisper-L8 task-stratified layer sister (semantic tasks may want L20–24).
- **EX08** (SMALL): PMA module internal spec — 1-layer cross-attn MHA + 1-layer MLP-4×.
- **EX14** (MED): `R-per-subject-linear` PopT-style sister falsifies "zero per-subject" headline against the obvious reviewer baseline.

### Corpus quality (CQ)

- **CQ01** (MED): audit BT 23 unverified film cut variants (Theatrical vs Extended); LotR-1/2 Extended already confirmed.
- **CQ02** (MED): broader BT transcript integrity audit (thor-ragnarok + lotr-1 truncation precedent; remaining 23 unaudited).
- **CQ03** (MED): generalize BTWordEvents split-parity test from speech-only to all 14 task labels.
- **CQ04** (MED): BT DK 80-standard vs 64-populated parcels — keep full vocab, diagnostic per-parcel population figure.
- **CQ06** (MED): per-movie BT MFA alignment QC (flag films < 0.8 cross-correlation as degraded).
- **CQ07** (MED): AJILE12 ECoG-density bias in Phase-1 per-electrode features — `R-no-ajile12-p1` sister.
- **CQ08** (BIG): AJILE12 anatomy unavailable — Phase-2 architecturally ineligible; document.
- **CQ11** (SMALL): D-cohort chronic-data acquisition timeline — confirm with Nanlin (feasible / blocked / theoretical).
- **CQ13** (MED): SWEC dedup ledger lock — `assert len(study.subject_ids) == 50` in SWECStudy.
- **CQ18** (SMALL): per-corpus electrode-labeling contract — `Study.get_electrode_support(subject_id)` base method.

### Cross-memo coherence (ARG)

- **ARG02** (MED): Phase-3 preflight tie-breaker rule conflict — align all memos on "prefer Goldstein within Δ=0.01, trust empirical if Δ > 0.01".
- **ARG09** (MED): Whisper layer cite drift — Goldstein measured medium L4 + decoder L3; v14 uses large-v3 L8 (extrapolation, not direct cite).

### Interp-output specs (VIS)

- **VIS13** ✅ **(2026-05-26)**: statistical power + multiple-comparison correction policy locked for the interp suite. **Correction = Benjamini-Hochberg FDR at q=0.05, applied PER family of tests.** Families pre-declared in methods: (a) parcel-connectivity tests (VIS01), (b) band re-discovery / band-attribution tests (VIS02 + 5th novel claim's per-band IG / ablation / clustering), (c) temporal-latency cascade tests (VIS03), (d) PMA-attention visualization tests (VIS04), (e) post-hoc subject-ID linear probe tests, (f) any future interp cells. Each figure caption states its family + q-threshold + N_tests + N_surviving-q=0.05. **Minimum detectable effect = Cohen's d ≥ 0.5 (medium) at 80% power post-FDR.** Under-powered cells (i.e. where the design can't reach d=0.5 at 80%) reported as \"descriptive, not inferential\" in caption + flagged in methods supplement with per-cell power calc. Cluster-based permutation (Maris-Oostenveld 2007) considered for spatially/temporally contiguous tests (parcel-connectivity, temporal-latency) but rejected as scope-creep — per-family FDR-BH is the standard neuroscience default (Genovese 2002, Nichols 2007) and the marginal stringency from cluster-perm doesn't outweigh the bookkeeping cost of mixed correction schemes across the interp suite. Methods supplement § Statistical methods: power calc tables per cell + family pre-declaration + reporting convention. **Verifiable as**: `from statsmodels.stats.multitest import multipletests` with `method='fdr_bh'` in `src/speech_decoding/analysis/interp_suite.py` (or equivalent); per-family declaration table in `paper/neuroprobe-hillclimb/methods/statistics.md`; under-powered-cell flag column in figure-caption helper.
- **VIS01–04, VIS05–16** (MED/SMALL): remaining VIS items lost in 2026-05-23 repatriation pass; surfaced as needed when interp suite implementation begins (Phase-4 / paper-writing time). Catalogue lives in `docs/archive/v14_blockers_audit_trail_2026_05_22.md`.

### Schedule realism (TIME)

- **TIME04** (BIG): critical-path serial-cascade table post-M0; if path > 70 days, Jul 4 fork mandatory.
- **TIME07** ✅ **(2026-05-26)**: **Jul 4 fork-gate = P2→P4 frozen-linear probe on 2 Neuroprobe tasks.** By Jul 4, run the P2 checkpoint through a P4 frozen-linear probe on 1 CSession multi-class task + 1 CSubject binary AUROC task. **Pass threshold: ≥ 0.60 CSession multi-class AND ≥ 0.55 CSubject binary AUROC** — 10pp below the headline submission gate (0.667 / 0.628) to leave headroom for the Phase-3 distillation lift. **Below threshold → fork to P1-P2-only fallback paper** ("parcel-routed JEPA encoder, no cross-modal distillation" framing per `memory/project_neurips_workshop_target_2026_05_20.md`). **Above threshold → commit to full Phase 3 thesis.** Probe machinery reuses S06's callback infrastructure; ~1 GPU-day overhead at Jul 4 gate evaluation. **Co-gate (floor)**: P2 must have completed ≥ 20k of 40k steps by Jul 4; below → fork regardless of probe (catches catastrophic schedule slip). **Probe task choice**: pick from the pre-published gate-set (EV08); choose 1 task whose iEEG-SOTA is between 0.60 and 0.70 so the gate has signal (not saturated, not floor). **Verifiable as**: `tools/v14_jul4_fork_gate.py` script that loads the latest P2 checkpoint, runs the 2-task frozen-linear probe, prints PASS/FAIL + threshold values; `R-time07-fork-gate-jul4` cron entry in ScheduleWakeup or manual Jul-4 task; fallback-paper framing locked in `memory/project_neurips_workshop_target_2026_05_20.md` (already exists per CLAUDE.md `MEMORY.md`).
- **TIME08** (MED): confirm co-author Aug 1–29 availability (Greg + Zac) now; identify proxy if unavailable.

### Operational cross-refs (HB → B16)

- **HB10** (SMALL): pre-cache Whisper-large-v3 to `/work/ht203/cache/whisper/`; set `HF_HOME` in Exca env. Bullet into B16.

---

## What's locked (so we know what's not on this list)

- Architecture pipeline ❶→❼ (post-v3 §0)
- d=256, heads=8, N=6, L=6, M=4 (defaults; **N and L are sweep placeholders, not principled**, per recipe §1 amendment 2026-05-23)
- 2 cross-attns @ {0, 3}
- Multi-STFT front-end (3 windows + 256-sample hop = 8 Hz frame rate per B20 v4 lock 2026-05-24 + ⅓-octave 30-bin filterbank, raw |STFT| value axis per 5/25 swap; F-log-amplitude sister re-enables `log` via `apply_log=True`)
- Single-teacher Whisper-L8 (DINOv3 dropped)
- Phase-asymmetric readout (triangular-pool Phase 3 / flatten Phase 4)
- Per-corpus valid-bin mask (6-row spec)
- Phase 1 / Phase 2 / Phase 3 corpus (post-AJILE12 lock 5/22 evening)
- DK-first parcel routing, `log(support+ε)`, ε = 1e-2
- Submission gate (≥ 0.667 CSession multi-class AND ≥ 0.628 CSubject binary AUROC)
- L.1 / L.2 / L.3 / L.4 / shaft+depth / Tier-C frozen Stage-0 contracts
- PMA k=1 over parcels as the shared parcel-collapse head


---

## Audit-trail appendices archived

The 2nd / 3rd / 4th / 5th-pass discovery audits (lens-specific fan-outs that generated the 39-item lock-and-delete batch + 13 FLAGs + B17 promotion) have been moved to [`docs/archive/v14_blockers_audit_trail_2026_05_22.md`](../archive/v14_blockers_audit_trail_2026_05_22.md). The live doc above now lists only unresolved gaps; closed items live in `training_recipe.md §9`.
