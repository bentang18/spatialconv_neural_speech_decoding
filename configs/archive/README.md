# Archived Configs

- `historical_baselines/`: old supervised and LOPO baselines kept for reproduction
- `sweeps/`: one-off CE/input sweeps and comparison variants
- `negative_pivots/`: configs from directions that are no longer on the active path
